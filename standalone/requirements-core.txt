# ── standalone 최소 구동 (웹앱 부팅 + 순수 파이썬 도구) ───────────────
# 이것만 설치해도 사이트는 뜨고, 무거운 라이브러리는 각 도구 실행 시점에만 필요.
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-WTF==1.3.0
WTForms==3.2.2
Werkzeug==3.1.8

# ── 자주 쓰는 도구용 (대부분 Windows/Mac 휠 제공) ─────────────────────
# 필요 없으면 주석 처리해도 부팅에는 영향 없음.
Pillow
pypdf
folium
geopy
reportlab
openpyxl
requests
beautifulsoup4
python-dateutil
defusedxml
pycryptodome
pefile
oletools
extract-msg
simplekml
polyline
dpkt

# ── 리눅스 전용/빌드 필요 (Windows 설치 어려움) ──────────────────────
# 디스크/파일시스템/타임라인/메모리 분석 도구는 아래가 있어야 동작.
# 가급적 리눅스(or WSL)에서 전체 requirements.txt 로 설치할 것.
#   pytsk3, libewf-python, libfsntfs-python 등 libyal 계열
#   plaso, volatility3, yara-python, pytesseract, pyzbar
