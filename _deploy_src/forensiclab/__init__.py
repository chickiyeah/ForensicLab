"""ForensicLab core library.

산재한 일회성 스크립트(crawfile.py, anl.py 등)에 중복된 포렌식 로직을
재사용 가능한 모듈로 모으는 프레임워크 패키지.

모듈:
- :mod:`forensiclab.carving` — 시그니처 기반 파일 카빙 코어.
- :mod:`forensiclab.hashing` — 파일/바이트 해시 계산·검증 코어.
- :mod:`forensiclab.mbr` — MBR 파티션 테이블 파싱(읽기 전용) 코어.
- :mod:`forensiclab.mqtt` — MQTT(IoT pub/sub 메시징) Control Packet 파싱 코어.
- :mod:`forensiclab.coap` — CoAP(IoT REST·RFC 7252) 메시지 파싱 코어.
- :mod:`forensiclab.modbus` — Modbus/TCP(산업 제어 ICS/SCADA·TCP 502) 메시지 파싱 코어.
- :mod:`forensiclab.dnp3` — DNP3 데이터 링크 프레임 파싱(IEEE 1815·TCP/UDP 20000; :mod:`forensiclab.modbus` 의 **전력·수도 SCADA 형제** — 광역 RTU·IED 를 마스터가 폴링·제어하는 북미 전력망 표준 제어 평면, Industroyer/CRASHOVERRIDE 가 차단기를 OPERATE 로 연 평면; 전통적으로 무인증·평문이라 20000 노출 자체가 중요 인프라 정황·SELECT/OPERATE/WRITE 한 번이 물리 차단기·밸브 조작; 와이어 데이터 링크 8바이트 헤더+CRC: 시작 동기 ``0x05 0x64`` 고정 강한 오탐 가드·Length(5~255, 5+사용자 데이터)·Control(DIR/PRM/FCB/FCV+하위 4비트 링크 함수—PRM 따라 RESET_LINK_STATES·REQUEST_LINK_STATUS 정찰·CONFIRMED/UNCONFIRMED_USER_DATA 데이터 운반 또는 ACK/NACK/LINK_STATUS)·Destination/Source little-endian RTU 주소(장치 열거·마스터↔아웃스테이션 방향 :mod:`forensiclab.flows`); 사용자 데이터는 전송 헤더+응용 제어+응용 함수 코드로 시작(첫 16B 블록 내)—1 READ 정찰·2 WRITE·3 SELECT·4 OPERATE·5 DIRECT_OPERATE 물리 조작(``is_control``)·129 RESPONSE·130 UNSOLICITED_RESPONSE; ``link_function_name``/``application_function_name``·``is_master``(DIR·PRM)·``is_request``/``is_response`` :mod:`forensiclab.timeline`; 8B 미만·동기≠0x0564·Length<5·예약 링크 함수 오탐 가드 ``None``·CRC 무검증·사용자 데이터 ``payload_offset`` 으로 가리키고 응용 함수만 디코드·잘리면 ``truncated``·offset 지원) 코어.
- :mod:`forensiclab.s7comm` — S7comm 지멘스 S7 PLC 통신 파싱(TPKT/COTP 위 S7 Communication·TCP 102; :mod:`forensiclab.modbus` 가 "Stuxnet·Industroyer·TRITON 표적 ICS 공격"을 언급하고 :mod:`forensiclab.dnp3`·:mod:`forensiclab.iec104` 가 전력망 SCADA 형제였다면 **Stuxnet 이 실제로 말한 와이어가 바로 S7comm** — 지멘스 SIMATIC S7-300/400/1200/1500 PLC 가 STEP 7 엔지니어링 워크스테이션·HMI·SCADA 와 주고받는 사실상 표준; Modbus/DNP3/IEC104 가 센서값·차단기 제어 평면이라면 S7comm 은 **PLC 의 메모리·로직(블록) 자체를 읽고 쓰는** 더 깊은 평면 — Stuxnet 은 이 평면으로 원심분리기 제어 OB/FC 블록을 다운로드(주입)하고 정상값을 재생; 클래식 S7comm 은 무인증·평문이라 TCP 102 노출 자체가 PLC 엔지니어링 평면 정황·Write Var 한 번이 변수 조작·PLC Control/Stop 한 번이 CPU 기동·정지·Download 한 번이 PLC 로직 주입(Stuxnet 수법); 와이어 3계층 캡슐화(big-endian) TPKT(RFC 1006 4B: Version 항상 0x03 강한 오탐 가드·Length)→COTP(ISO 8073: LI+PDU Type, S7comm 은 DT Data 0xF0 에만 실림—연결 설정 CR 0xE0/CC 0xD0 은 페이로드 없어 프레이밍 가드 None, COTP 헤더 = LI+1)→S7comm 헤더(Protocol ID 항상 0x32 강한 오탐 가드·ROSCTR 1 Job 요청·2 Ack·3 Ack_Data 응답·7 Userdata·Redundancy ID reserved·PDU Reference 요청↔응답 상관 modbus transaction_id·coap message_id 대응·Parameter Length·Data Length, Ack/Ack_Data 면 Error Class+Code 2B 추가 헤더 12B 그 외 10B)+파라미터 첫 바이트 함수 코드(0x04 Read Var·0x05 Write Var 메모리 정찰/조작·0x1A~0x1F Download/Upload 블록 로직 주입·추출=Stuxnet·0x28 PLC Control 기동·0x29 PLC Stop 정지·0xF0 Setup Communication); **노출된 PLC 엔지니어링 평면**(TCP 102 자체가 S7 정황·rosctr_name 평면)·**읽기 vs 쓰기 vs 로직 주입**(is_read/is_write 메모리·is_control/is_stop CPU 상태·is_download/is_upload 블록=Stuxnet 핵심 단계)·**세션·타임라인**(pdu_reference Job→Ack_Data :mod:`forensiclab.timeline`)·**오류**(Ack/Ack_Data error_class/error_code 접근 거부·항목 없음=주소 열거 정찰) 단서; rosctr_name/function_name 공개 헬퍼·TPKT Version≠0x03·COTP≠DT Data·Protocol ID≠0x32·ROSCTR∉{1,2,3,7}·헤더 절단이면 오탐 가드 None·파라미터/데이터 본문 parameter_offset/data_offset 으로만 가리키고 함수 코드만 디코드·잘리면 truncated·읽기 전용·offset 지원) 코어.
- :mod:`forensiclab.iec104` — IEC 60870-5-104 전력망 SCADA 원격제어 파싱(IEC 104·TCP 2404; :mod:`forensiclab.dnp3` 의 **유럽·전 세계 형제** — 제어 센터가 변전소(controlled station)의 차단기·아날로그 설정값을 폴링·제어하는 telecontrol 평면, IEC 101(직렬)의 TCP/IP 적응판; **Industroyer/CRASHOVERRIDE 가 DNP3·IEC 101 외에 IEC 104 전용 모듈로 차단기를 C_SC/C_DC 로 연 평면**; 무인증·평문이라 2404 노출 자체가 중요 인프라 정황·C_SC/C_DC/C_RC/C_SE 명령 한 번이 물리 차단기·밸브·설정값 조작; 와이어 APCI(시작 ``0x68`` 고정 강한 오탐 가드·APDU Length 4~253·제어 4옥텟)+ASDU: 제어 첫 옥텟 하위 2비트로 **3종 포맷**—I-format(bit0=0 정보 전송, N(S)/N(R) 15비트 + ASDU)·S-format(``0x01`` 수신 확인 N(R))·U-format(하위 2비트 ``11`` STARTDT/STOPDT/TESTFR act/con 데이터 전송 개시·중지·keep-alive ``u_function_name``); I-format ASDU 6바이트 헤더—Type ID(1 M_SP_NA_1 단일점·45 C_SC_NA_1 단일명령·46 C_DC 이중명령·47/48~51 조정·설정값 물리 조작 ``is_control``·100 C_IC_NA_1 총괄심문·102 C_RD 읽기 정찰 ``is_interrogation``)·VSQ(SQ+정보객체 수)·COT(전송 원인 6비트 6 act·7 actcon·10 actterm·20 inrogen + P/N ``is_negative`` 거부·T ``is_test``)·발신자 주소·Common Address little-endian 변전소 주소(장치 열거 :mod:`forensiclab.flows`); ``type_name``/``cause_name``/``u_function_name`` 공개 헬퍼·N(S)/N(R) 전송 순서·세션 STARTDT_ACT→CON :mod:`forensiclab.timeline`; 6B 미만·시작≠0x68·Length<4 또는>253 오탐 가드 ``None``·정보객체 본문 ``payload_offset`` 으로만 가리키고 ASDU 헤더만 디코드·잘리면 ``truncated``·읽기 전용·offset 지원) 코어.
- :mod:`forensiclab.bacnet` — BACnet/IP 빌딩 자동화 제어망 파싱(ASHRAE 135/ISO 16484-5·UDP 47808; :mod:`forensiclab.modbus`·:mod:`forensiclab.dnp3`·:mod:`forensiclab.iec104`·:mod:`forensiclab.s7comm` 이 공장·발전소·전력망의 OT 제어 평면(센서·차단기·PLC)이었다면 BACnet 은 그 **건물 자동화 형제** — HVAC 공조·조명·엘리베이터·**출입 통제**·화재 경보를 묶는 사실상 표준, ForensicLab IoT 센서 모니터링과 직접 맞닿고 **인터넷 노출 BACnet/IP(UDP 47808)** 는 Shodan 단골이라 병원·데이터센터·캠퍼스 공조·도어락이 무인증 노출; 클래식 BACnet 은 인증·암호화 사실상 없음(BACnet/SC 이전 평문)이라 47808 노출 자체가 빌딩 제어 평면 정황·Who-Is 한 번이 장치 열거·WriteProperty 한 번이 온도 설정점·도어 릴레이 물리 조작·DeviceCommunicationControl 한 번이 장치 통신 마비 DoS·ReinitializeDevice 한 번이 콜드스타트 재부팅; 와이어 3계층(big-endian) BVLC(Type 항상 0x81 강한 오탐 가드·Function 0x0A Original-Unicast·0x0B Original-Broadcast·0x04 Forwarded-NPDU 앞 6B B/IP 출처주소·0x00 BVLC-Result 2B 결과코드 NPDU없음 등·Length)→NPDU(Version 항상 0x01 강한 오탐 가드·Control bit7 망계층메시지 APDU없음·bit5 목적지 DNET/DLEN/DADR·bit3 출처 SNET/SLEN/SADR·bit2 응답기대·bits1-0 우선순위, 라우팅이면 dnet/snet 망번호 세그먼트 열거)→APDU(첫바이트 상위4비트 PDU Type 0 Confirmed-Request·1 Unconfirmed-Request·2 SimpleACK·3 ComplexACK·5 Error·6 Reject·7 Abort+하위4비트 플래그·Invoke ID 요청응답 상관 modbus transaction_id·s7comm pdu_reference 대응·Service Choice—Unconfirmed 8 who-Is 정찰/0 i-Am 응답·Confirmed 12 readProperty·14 readPropertyMultiple·26 readRange 정찰/15 writeProperty·16 writePropertyMultiple 물리조작/17 deviceCommunicationControl·20 reinitializeDevice DoS); **노출된 빌딩 제어 평면**(UDP 47808 자체가 BACnet 정황·bvlc_function_name)·**장치 열거**(is_who_is/is_i_am)·**읽기 vs 쓰기**(is_read 정찰·is_write 설정점·도어 물리조작)·**마비·재부팅**(is_device_control DoS·증거 인멸)·**세션·타임라인**(invoke_id 요청→응답 :mod:`forensiclab.timeline`)·**오류**(is_error Error/Reject/Abort) 단서; bvlc_function_name/pdu_type_name/service_name 공개 헬퍼·BVLC Type≠0x81·Function 미지·NPDU Version≠0x01 면 오탐 가드 None·NPDU/APDU 본문 npdu_offset/apdu_offset 으로만 가리키고 PDU Type·Invoke ID·Service Choice 만 디코드·잘리면 truncated·읽기 전용·offset 지원) 코어.
- :mod:`forensiclab.strings` — 바이너리 ASCII/UTF-16LE 문자열 추출 코어.
- :mod:`forensiclab.logparse` — Apache 접근 로그 파싱·공격 패턴 탐지 코어.
- :mod:`forensiclab.exif` — JPEG EXIF GPS 좌표·카메라 메타데이터 추출 코어(순수 stdlib).
- :mod:`forensiclab.entropy` — 바이트 Shannon 엔트로피 분석(암호화/압축 영역 탐지) 코어.
- :mod:`forensiclab.filetype` — 매직 바이트 기반 파일 타입 식별·확장자 위조 탐지 코어.
- :mod:`forensiclab.timeline` — 시각 있는 사건 정렬·구간 필터·출처별 묶음(타임라인 재구성) 코어.
- :mod:`forensiclab.pcap` — libpcap(.pcap) 캡처 파일 헤더·패킷 레코드 파싱(순수 stdlib) 코어.
- :mod:`forensiclab.netdissect` — 원시 패킷 바이트 Ethernet/IPv4/TCP·UDP 헤더 해석(순수 stdlib) 코어.
- :mod:`forensiclab.flows` — 해석된 패킷을 양방향 흐름(conversation)으로 누적(흐름 재구성) 코어.
- :mod:`forensiclab.dns` — UDP 53 페이로드를 DNS 메시지(헤더+질문)로 파싱(C2/터널링/DGA 단서) 코어.
- :mod:`forensiclab.http` — TCP 페이로드를 HTTP 요청(요청 라인+헤더)으로 파싱(C2/유출 단서) 코어.
- :mod:`forensiclab.tls` — TLS ClientHello/ServerHello 파싱(SNI·ALPN·협상 버전·핑거프린트 단서) 코어.
- :mod:`forensiclab.ja3` — ClientHello 협상 파라미터를 JA3 핑거프린트(문자열+MD5)로 환원(도메인 무관 멀웨어 식별) 코어.
- :mod:`forensiclab.ja3s` — ServerHello 를 JA3S 핑거프린트(문자열+MD5)로 환원(JA3 와 짝지어 C2 서버 식별) 코어.
- :mod:`forensiclab.ssh` — SSH 버전 식별 배너 파싱(softwareversion·protoversion 추출, 취약 버전·자동화 툴 식별) 코어.
- :mod:`forensiclab.hassh` — SSH_MSG_KEXINIT 알고리즘 목록을 HASSH 핑거프린트(문자열+MD5)로 환원(배너 위조와 무관한 SSH 구현 식별) 코어.
- :mod:`forensiclab.icmp` — IPv4 ICMP 제어 메시지 파싱(echo id/seq·오류 메시지 원본 패킷, ping 스윕/터널/연결 실패 단서) 코어.
- :mod:`forensiclab.arp` — ARP 패킷 파싱(sender/target MAC·IP, gratuitous 판별, ARP 스푸핑/MITM 단서) 코어.
- :mod:`forensiclab.dhcp` — DHCP/BOOTP 메시지 파싱(hostname·요청 IP·vendor class·param-req-list, 호스트 식별·DHCP 핑거프린트 단서) 코어.
- :mod:`forensiclab.ntp` — NTP 패킷 파싱(mode·stratum·reference ID·timestamp, mode 6/7 증폭 DDoS·호스트 상관·시계 편차 단서) 코어.
- :mod:`forensiclab.tftp` — TFTP 메시지 파싱(opcode·filename·mode·옵션·블록·오류, 멀웨어 스테이징·설정/펌웨어 유출 단서) 코어.
- :mod:`forensiclab.syslog` — syslog 전송 파싱(PRI facility/severity·hostname·app/proc·구조화 데이터, 원격 로깅·인증 실패·로그 위조 단서) 코어.
- :mod:`forensiclab.radius` — RADIUS AAA 파싱(code·identifier·authenticator·TLV 속성, User-Name·NAS-IP·Calling-Station-Id, 인증 실패/브루트포스·호스트 상관·세션 타임라인 단서) 코어.
- :mod:`forensiclab.tacacs` — TACACS+ 평문 헤더 파싱(RFC 8907·TCP 49; :mod:`forensiclab.radius` 의 **TCP 기반 사촌** AAA — 시스코 장비 관리용으로 라우터·스위치 관리자 로그인·``enable``·명령 단위 인가/과금에 표준; RADIUS 가 인증·인가를 합치는 데 비해 **A·A·A 를 분리**해 명령 단위 권한·감사; RADIUS 가 패스워드만 가리는 것과 달리 **본문 전체를 MD5 의사 패드 XOR 로 난독화**(공유 비밀 필요)라 :mod:`forensiclab.esp`·:mod:`forensiclab.wireguard` 처럼 **평문 12바이트 헤더만** 풀고 본문은 ``payload_offset`` 으로만 가리킴(비밀 무복호); 와이어(big-endian) version<1: major 상위4비트 항상 0xc·minor 하위4비트>+type<1: 1 Authentication·2 Authorization·3 Accounting>+seq_no<1: 클라 홀수·서버 짝수, 1=개시>+flags<1: bit0 0x01 UNENCRYPTED 본문 평문—미운영 권고·노출/디버그 정황·bit2 0x04 SINGLE_CONNECT>+session_id<4: AAA 대화 상관 키, RADIUS Acct-Session-Id·WireGuard index·ESP SPI 대응, :mod:`forensiclab.flows` IP 쌍 안에서 한 로그인 세션 못 박음>+length<4: 본문 길이>; **장비 관리 AAA 존재·표적**(노출된 TCP 49 자체가 관리 평면 정황·RADIUS 1812·SNMP 형제)·**A·A·A 단계 식별**(``is_authentication``/``is_authorization``/``is_accounting`` 로그인→명령 권한→명령 실행 :mod:`forensiclab.timeline`)·**세션 상관·브루트포스**(같은 IP 쌍 새 ``session_id`` 쏟아지고 ``seq_no`` 1~3 에서 끊김=패스워드 스프레이, RADIUS Access-Reject 반복 대응)·**평문 플래그**(``is_unencrypted`` 자격증명 그대로 노출 강한 단서) 단서; 12B 미만·major≠0xc·type 미정의면 오탐 가드 ``None``; 난독화 본문 무복호·offset 지원) 코어.
- :mod:`forensiclab.diameter` — Diameter 고정 헤더 파싱(RFC 6733·TCP/SCTP 3868; :mod:`forensiclab.radius` 의 **IETF 공식 후계자** AAA·:mod:`forensiclab.tacacs` 형제 — 신뢰 전송(TCP/SCTP) 위에서 동작하고 무엇보다 **이동통신 코어망 신호 평면**(LTE/EPC S6a/S6d MME↔HSS 가입자 인증·위치·IMS Cx·정책 Gx)이라 :mod:`forensiclab.gtp`(사용자/베어러 터널)와 **짝을 이루는 제어 평면**; AVP(RADIUS TLV 후손) 본문은 :mod:`forensiclab.tacacs`·:mod:`forensiclab.gtp` 처럼 **고정 20바이트 헤더만** 풀고 ``payload_offset`` 으로만 가리킴; 와이어(big-endian) version<1: 항상 1>+message_length<3: 헤더+AVP 전체, 4의 배수·20 이상>+command_flags<1: bit7 0x80 R=Request·bit6 0x40 P=Proxiable·bit5 0x20 E=Error·bit4 0x10 T=재전송, 하위4비트 예약 0>+command_code<3: 요청/응답 공통—257 CER/CEA·280 DWR/DWA·272 CCR/CCA 과금·316 ULR/ULA S6a 위치갱신·318 AIR/AIA 인증벡터>+application_id<4: 0 기본·4 Credit-Control·16777251 3GPP S6a/S6d·16777216 3GPP Cx 어느 인터페이스인지 못 박음>+hop_by_hop_id<4: 인접 피어 요청↔응답 정합>+end_to_end_id<4: 종단 간 중복탐지·정합, 둘이 :mod:`forensiclab.flows` IP 쌍 안 트랜잭션 상관 키>; **코어망 신호 평면 존재·표적**(노출된 3868 자체가 통신사 코어망 정황·Diameter 라우팅 공격=가입자 위치추적·SMS 가로채기·과금 우회 SS7 의 IP 후신·application_id S6a 면 HSS↔MME 강한 단서)·**명령 식별**(``command_name``+``is_request``/``is_answer`` CER 능력교환→DWR 워치독→ULR/AIR 위치·인증 조회 :mod:`forensiclab.timeline`)·**오류·재전송**(``is_error`` E 폭주=인증/라우팅 실패 RADIUS Access-Reject 대응·``is_retransmit`` T 빈발=전송 불안정)·**트랜잭션 상관**(hop_by_hop/end_to_end 프록시 체인 가로질러 요청·응답 짝) 단서; 20B 미만·version≠1·길이<20 또는 4 비배수·예약 플래그 비트면 오탐 가드 ``None``; AVP 본문 무파싱·offset 지원) 코어.
- :mod:`forensiclab.sctp` — SCTP 공통 헤더·청크 파싱(RFC 4960·IP proto 132; :mod:`forensiclab.diameter` 가 "TCP/SCTP 3868 위에서 동작"할 때 그 **전송 계층 자체** — TCP·UDP 의 형제격 4세대 전송, **멀티스트리밍**(head-of-line 회피)·**멀티호밍**(다중 IP 경로 전환) 추가, SS7 의 IP 후신 SIGTRAN M3UA(2905)·:mod:`forensiclab.diameter`(3868)·WebRTC 데이터채널이 그 위에 실림; **12바이트 공통 헤더만** 풀고 청크는 :mod:`forensiclab.diameter`·:mod:`forensiclab.gtp` 처럼 값 무파싱·타입/플래그/길이만 훑어 열거하고 ``payload_offset`` 으로만 가리킴; 와이어(big-endian) source_port+dest_port<3868 Diameter·2905 M3UA 면 코어망 신호 정황>+verification_tag<4: association 식별, INIT 패킷에선 반드시 0—아니면 스푸핑/주입, :mod:`forensiclab.esp` SPI·:mod:`forensiclab.gtp` TEID 처럼 :mod:`forensiclab.flows` IP 쌍 가로질러 association 상관 키>+checksum<4: 전체 CRC32c, 미검증>, 이어서 청크들 chunk_type<1: 0 DATA·1 INIT·2 INIT-ACK·3 SACK·4 HEARTBEAT·6 ABORT·7 SHUTDOWN·10/11 COOKIE-ECHO/ACK>+chunk_flags<1>+chunk_length<2: 헤더 4 포함, 다음 청크는 4바이트 경계 패딩>; **신호 평면 전송 식별**(흔치 않은 SCTP 노출 자체가 코어망·통신 인프라 정황)·**연결 수립 흐름**(``is_init`` INIT→INIT-ACK→COOKIE-ECHO→COOKIE-ACK 4단계 :mod:`forensiclab.timeline`·INIT 만 쏟아지면 INIT 플러딩/sctpscan)·**리셋·종료**(``is_abort`` ABORT 폭주=강제 절단·``is_shutdown`` 정상 종료 TCP RST/FIN 대응)·**association 상관**(verification_tag·``has_chunk``/``chunk_names`` 로 한 패킷 내 제어+데이터 청크 혼재 열거) 단서; 12B+첫 청크 4B 미만·첫 청크 길이<4 면 오탐 가드 ``None``·절단 캡처면 담을 수 있는 청크까지만·체크섬 무검증·offset 지원) 코어.
- :mod:`forensiclab.snmp` — SNMP v1/v2c 파싱(BER/ASN.1: version·community·PDU·varbind OID, 기본 community·Get/GetBulk 정찰·SetRequest 설정 변조·트랩 단서) 코어.
- :mod:`forensiclab.nbns` — NBNS(NetBIOS Name Service) 파싱(헤더·NetBIOS 1차 인코딩 이름·suffix 서비스, LLMNR/NBT-NS 포이즈닝(Responder)·NBSTAT 정찰·이름 등록 스푸핑 단서) 코어.
- :mod:`forensiclab.llmnr` — LLMNR(Link-Local Multicast Name Resolution) 파싱(RFC 4795, UDP 5355; :mod:`forensiclab.dns` 와 **같은 와이어 포맷** 공유·:mod:`forensiclab.nbns`(NBT-NS)와 **이름 해석 포이즈닝의 한 쌍** — DNS 로 못 푼 이름(오타·없는 호스트·``wpad``)이 멀티캐스트로 흘러나오는 트리거 표면; 12바이트 DNS 헤더 + 표준 DNS 라벨 이름(NBNS 1차 인코딩 아님), 플래그 비트 재해석(C=Conflict·T=Tentative)·Opcode 0 만 정의하므로 비-0 거부 오탐 가드; **Responder/Inveigh 포이즈닝**: 질의(QR=0)와 곧이은 응답(QR=1) 상관·한 응답자가 무관한 여러 이름을 모두 자기 IP 로 답하는 패턴·답변 A/AAAA RR 의 ``address``(``answer_addresses``)가 공격자 주장 IP=곧 NTLM 인증이 향할 목적지(:mod:`forensiclab.smb` NTLMSSP 와 상관)·**WPAD 하이재킹**(``has_wpad_query``/``LlmnrQuestion.is_wpad`` 프록시 자동탐색 탈취)·질의된 이름 자체가 내부 호스트명/공유명/오타 정찰(``queried_names``) 단서; 압축 포인터 방어적 추적(무한 루프 가드)·질문/답변 잘리면 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.ssdp` — SSDP(UPnP 탐색) 파싱(HTTP-유사 시작줄·헤더, M-SEARCH/NOTIFY·ST/NT·USN·LOCATION·SERVER, SSDP 증폭 반사 DDoS·IoT 자산 정찰·악성 광고 단서) 코어.
- :mod:`forensiclab.ftp` — FTP 제어 채널 파싱(RFC 959: 명령 동사/인자·3자리 응답 코드, USER/PASS 평문 자격증명·PORT/PASV 데이터 채널 상관·RETR/STOR 유출/스테이징·530 인증 실패 단서) 코어.
- :mod:`forensiclab.smtp` — SMTP 파싱(RFC 5321: 명령 동사/인자·3자리 응답 코드, AUTH PLAIN Base64 평문 자격증명·MAIL/RCPT 봉투 주소·VRFY/EXPN 사용자 열거·STARTTLS 다운그레이드·535 인증 실패 단서) 코어.
- :mod:`forensiclab.pop3` — POP3 파싱(RFC 1939: 명령 동사/인자·+OK/-ERR 상태 표시자, USER/PASS 평문 자격증명·APOP 다이제스트+배너 오프라인 크래킹·AUTH PLAIN Base64 자격증명·RETR/TOP 메일 유출·DELE 증거 정리·-ERR 인증 실패 단서) 코어.
- :mod:`forensiclab.imap` — IMAP4rev1 파싱(RFC 3501: 태그·동사·인자 / 태그·무태그(*)·연속(+) 응답, LOGIN 평문 자격증명·AUTHENTICATE PLAIN Base64 자격증명·SELECT/EXAMINE 사서함 접근·FETCH(UID FETCH) 메일 유출·STORE \\Deleted+EXPUNGE 증거 정리·NO/BAD 인증 실패/프로토콜 오류 단서) 코어.
- :mod:`forensiclab.telnet` — Telnet 파싱(RFC 854/855·1091/1572: 바이트 지향 IAC 제어 시퀀스를 평문 데이터/옵션 협상(WILL·WONT·DO·DONT)·부협상(SB…SE)·단순 명령으로 분리, 평문 세션·자격증명 노출·TERMINAL-TYPE/NEW-ENVIRON 핑거프린트·IoT 브루트포스 단서) 코어.
- :mod:`forensiclab.irc` — IRC 파싱(RFC 1459/2812: [prefix] 명령/숫자응답 + 인자/trailing, nick!user@host 발신자 분해, PASS/OPER 평문 서버 자격증명·NICK/USER 봇 신원·명명 규칙·JOIN/PRIVMSG/NOTICE C2 채널·명령 전달·433/464/465 접속 실패 단서, 봇넷 C2 고전 백본) 코어.
- :mod:`forensiclab.finger` — Finger 질의 파싱(RFC 1288: [/W] [username] [@host...] 한 줄, 사용자 열거·빈 질의 전원 목록 대량 수집·user@host@host 전달/릴레이 피벗·/W verbose 정보 공개 확대 단서, SMTP VRFY/EXPN 계열 정찰) 코어.
- :mod:`forensiclab.nntp` — NNTP 파싱(RFC 3977/977: 명령 동사/인자·3자리 응답 코드, AUTHINFO USER/PASS 평문 자격증명·AUTHINFO SASL PLAIN Base64 자격증명·GROUP 뉴스그룹 접근·ARTICLE/BODY 글 유출·POST/IHAVE/TAKETHIS 주입·배포 은닉 채널·LIST/NEWGROUPS/NEWNEWS 열거·481/482 인증 실패/순서 오류 단서, SMTP/POP3 메일 형제) 코어.
- :mod:`forensiclab.ident` — Ident 파싱(RFC 1413: ``port,port`` 질의 / ``port,port:resp-type:add-info`` 응답, USERID 연결→사용자 귀속(흐름을 계정에 연결, IRC 서버 관례 질의)·위조 가능 자기 신고·HIDDEN-USER 노출 거부 회피·INVALID-PORT 존재 않는 연결 탐침·NO-USER 상관 실패 단서, finger/irc 형제) 코어.
- :mod:`forensiclab.whois` — WHOIS 파싱(RFC 3912: ``[플래그...] <대상>`` 한 줄, 대상을 IPv4/IPv6/ASN/도메인/핸들로 형태 분류·인프라 정찰/귀속(C2 인프라를 등록 주체에 연결)·``-h``/``@`` 서버 리다이렉션 재귀/피벗·와일드카드 대량 수집 단서, finger/ident 형제 평문 정보 공개 질의) 코어.
- :mod:`forensiclab.rlogin` — rlogin 시작 문자열 파싱(RFC 1282: 선두 NUL + ``client-user``·``server-user``·``terminal-type/speed`` NUL 종단 4-필드, 단일 ``\\x00`` 서버 ACK, 무암호 신뢰 기반(.rhosts/hosts.equiv) 인증·출발/목적 두 계정 동시 귀속·계정 교차/root 목표 측면 이동·터미널 핑거프린트 단서, telnet/ident/finger 형제 고전 r-services 평문) 코어.
- :mod:`forensiclab.rcmd` — BSD r-services 원격 명령 실행(rsh 514/rexec 512) 파싱(선두 stderr 포트 + NUL 종단 필드, 서버 ``\\x00`` ACK/``\\x01``+메시지 오류; 실행 명령 포착 귀속·rexec 평문 비밀번호·rsh 무암호 .rhosts 신뢰·이중/교차/root 목표 측면 이동·별도 stderr 자동화 단서, telnet/ftp 평문 형제) 코어.
- :mod:`forensiclab.rdp` — RDP 연결 요청(X.224 Connection Request) 파싱(MS-RDPBCGR: TPKT+X.224 CR 프레이밍, ``Cookie: mstshash=`` 사용자 식별자 pre-auth 평문 귀속·``msts`` 브로커 라우팅 토큰 피벗·rdpNegReq requestedProtocols 보안 협상(PROTOCOL_RDP 단독=NLA 없는 표준 RDP 보안 레거시·BlueKeep 다운그레이드, HYBRID/HYBRID_EX=NLA)·Restricted Admin PtH 벡터 단서, telnet/rlogin/rcmd/ssh 원격 접속·측면 이동 형제) 코어.
- :mod:`forensiclab.vnc` — VNC(RFB) ProtocolVersion 핸드셰이크 파싱(RFC 6143 §7.1.1: 12바이트 ``RFB nnn.nnn\\n`` 평문 버전 줄, 원격 화면 제어 접속·측면 이동(RDP/SSH 형제)·``3.3`` 레거시 약한 인증(DES·비밀번호 8자) 다운그레이드·비표준 버전(ARD 003.889 등) 구현 핑거프린트 단서, SecurityType 인증 협상은 별도 단계) 코어.
- :mod:`forensiclab.mysql` — MySQL/MariaDB 초기 핸드셰이크(서버 인사·HandshakeV10) 파싱(4바이트 패킷 헤더 자동 감지 + protocol_version·NUL 종단 server_version·thread_id·능력 플래그·캐릭터셋·auth_plugin_name; 인증 이전 평문 배너성 인사라 server_version 빌드 핑거프린트(취약 버전 식별)·``5.5.5-``/``MariaDB`` 벤더 핑거프린트·``CLIENT_SSL`` 미광고 평문 자격증명 노출·``mysql_clear_password``/``mysql_old_password`` 약한 인증 플러그인(native_password deprecated) 단서, 노출된 3306 DB 서비스는 RDP/SSH/VNC 형제 원격 접속·측면 이동 표적) 코어.
- :mod:`forensiclab.postgres` — PostgreSQL 프론트엔드 시작 메시지(StartupMessage) 파싱(MySQL 과 반대로 **클라이언트가 먼저** 보내는 첫 패킷·관용 포트 5432; 1바이트 타입 태그 없는 length<4>+code<4> 프레이밍 + NUL 종단 key/value 파라미터; 인증 이전 평문이라 필수 ``user`` 사용자 귀속(흐름→DB 계정)·``database`` 대상 식별·``application_name`` 도구/드라이버 핑거프린트(sqlmap 등) 노출·SSLRequest(80877103)/GSSENCRequest 특수 코드 TLS 미요청(requests_tls=False) 평문 정황·CancelRequest(80877102) pid+secret 평문 노출 약한 대역외 제어 단서, 노출된 5432 DB 서비스는 MySQL/RDP/SSH/VNC 형제 원격 접속·측면 이동 표적) 코어.
- :mod:`forensiclab.redis` — Redis RESP 명령 파싱(관용 포트 6379; ``*N\\r\\n`` 배열 + ``$len\\r\\n`` Bulk String 프레이밍, 레거시 인라인 한 줄 형식 모두; ``AUTH`` 평문 자격증명(1인자/2인자 ACL 마지막 인자가 비밀번호)·``CONFIG SET dir``/``dbfilename`` 임의 파일 쓰기(RDB) RCE 1차 프리미티브(authorized_keys/웹셸/cron)·``SLAVEOF``/``REPLICAOF`` 악성 복제 모듈 동기화·``MODULE LOAD`` 모듈 적재 코드 실행·``FLUSHALL``/``FLUSHDB`` 데이터셋 삭제 증거 파괴 단서, 무인증 노출된 6379 는 MySQL/PostgreSQL 형제 DB 표적; 동사 글자 시작 검증 오탐 방지·truncated 본문 부분 파싱·offset 지원) 코어.
- :mod:`forensiclab.memcached` — memcached 텍스트 프로토콜 파싱(관용 포트 11211 TCP·UDP; 줄 단위 명령, 고전 텍스트 프로토콜은 무인증(SASL 은 바이너리 전용)이라 기본 노출 흔함; **UDP 반사·증폭 DDoS(memcrashed, 1.7Tbps급)** 8바이트 프레임 헤더(request_id·sequence·total_datagrams·reserved) 자동 감지·UDP 도착 ``stats``/``get`` 증폭 탐침 식별·``get``/``gets``/``gat`` 캐시 데이터(세션 토큰) 유출·``set``/``add``/``replace``/``append``/``prepend``/``cas`` 캐시 오염 주입(데이터 블록 포착, 잘리면 가용분까지)·``flush_all`` 캐시 전체 무효화 증거 파괴·DoS·``stats``/``version`` 정찰·핑거프린트 단서, 무인증 노출된 11211 은 Redis 형제 캐시 표적; 알려진 동사 집합 검증 오탐 방지·offset 지원) 코어.
- :mod:`forensiclab.mongodb` — MongoDB 와이어 프로토콜 메시지 파싱(관용 포트 27017·샤드 27018·config 27019; 16바이트 헤더 little-endian int32 messageLength·requestID·responseTo·opCode, 알려진 opCode 검증 오탐 방지; 무인증 기본 노출(2017 "MongoDB 랜섬" 무대) 표적; ``OP_QUERY``(2004) flags+cstring fullCollectionName("db.col")+skip/return+query 문서·``OP_MSG``(2013) flagBits+kind-0 본문 섹션 BSON 명령, 최소 stdlib BSON 최상위 워커로 명령 이름(첫 키)·대상 DB/컬렉션·``$db``·``mechanism`` 추출; ``isMaster``/``hello`` 핸드셰이크·정찰(토폴로지 유출)·``saslStart``/``saslContinue``/``authenticate`` 인증 흐름·``mechanism=PLAIN`` 평문 자격증명 다운그레이드(SCRAM 대비)·``find``/``getMore`` 데이터 유출·``drop``/``dropDatabase``/``insert``/레거시 OP_INSERT·OP_UPDATE·OP_DELETE 쓰기/증거 파괴·랜섬 단서, MySQL/PostgreSQL/Redis/memcached 형제 노출 DB 표적; BSON 잘리면 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.mssql` — Microsoft SQL Server TDS PRELOGIN 파싱(MySQL/PostgreSQL 에 이은 세 번째 관계형 DB 형제·관용 포트 1433; TDS 8바이트 헤더 type<1>(0x12=PRELOGIN)·status·UInt16 length(big-endian)·spid 자동 감지 후 옵션 토큰 테이블(토큰<1>+offset<2>+length<2>, ``0xFF`` 종단) 파싱; 인증/암호화 협상 **이전** 평문 단계라 ``ENCRYPTION`` 협상값(``ENCRYPT_NOT_SUP``/``ENCRYPT_OFF`` → 이어질 LOGIN7 사용자/비밀번호가 nibble-swap+XOR 0xA5 자명 난독화만 거친 사실상 평문, ``plaintext_credentials_likely``; MySQL CLIENT_SSL 미광고·PostgreSQL SSLRequest 부재 대응)·``VERSION`` 서버 빌드 핑거프린트(취약 패치 수준)·``INSTOPT`` 명명 인스턴스 평문·``FEDAUTHREQUIRED`` Azure AD/토큰 인증 경로 단서, 노출된 1433 DB 서비스는 RDP/SSH/VNC/MySQL/PostgreSQL 형제 원격 접속·측면 이동 표적; 알려진 PRELOGIN 타입 검증 오탐 방지·옵션 offset/length 범위 밖이면 건너뜀·잘리면 가용 옵션까지·offset 지원) 코어.
- :mod:`forensiclab.oracle` — Oracle Database TNS CONNECT 파싱(MySQL/PostgreSQL/MSSQL 에 이은 **네 번째 관계형 DB 형제**·리스너 관용 포트 1521; TNS 8바이트 헤더 packet_length<2 big-endian>·checksum·type<1>(0x01=CONNECT) 자동 감지 후 본문 고정 필드(version·version_compatible 등 UInt16)와 connect_data_length/offset 로 가리켜진 평문 *connect descriptor* 괄호 트리 추출; 인증(O3LOGON/O5LOGON) **이전** 평문 협상 단계라 ``SERVICE_NAME``/``SID`` 접속 대상 DB 인스턴스 평문 노출(TNS 리스너 서비스명 정찰·brute 결과)·``version`` Oracle 릴리스 세대 핑거프린트(취약 패치 수준; MySQL server_version·MSSQL VERSION 대응)·``ADDRESS`` HOST/PORT(REDIRECT 타입 5 기반 TNS poisoning/MITM 정황 토대)·CID 의 ``PROGRAM``/``HOST``/``USER`` 클라이언트 도구·발신 호스트명·OS 사용자 평문 귀속(PostgreSQL application_name 대응) 단서, 노출된 1521 은 RDP/SSH/VNC/MySQL/PostgreSQL/MSSQL 형제 원격 접속·측면 이동 표적; 알려진 TNS 타입 검증·CONNECT 만 인정 오탐 방지·descriptor offset 범위 검사·잘리면 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.ldap` — LDAP 메시지 파싱(RFC 4511, BER/ASN.1; :mod:`forensiclab.snmp` 와 TLV 디코딩 골격 공유·관용 포트 389/글로벌 카탈로그 3268; 최외곽 SEQUENCE{messageID INTEGER, protocolOp [APPLICATION n]} 자동 감지·APPLICATION 클래스·음수 messageID 거부 오탐 방지; **BindRequest simple 인증 = DN(``name``)+비밀번호 평문 OCTET STRING 노출**(``CN=admin,DC=corp`` + 비밀번호 통째, FTP USER/PASS·MSSQL 평문 계열; 빈 DN+빈 비밀번호=익명 bind 정찰 진입점)·SASL ``mechanism``(GSSAPI 등) 식별·BindResponse ``resultCode=49``(invalidCredentials) 패스워드 스프레이/브루트포스 신호(RADIUS Access-Reject 대응)·SearchRequest ``baseObject``/``scope``(wholeSubtree=2 대량 워킹)/요청 ``attributes`` 중 ``userPassword``/``unicodePwd``/``msDS-ManagedPassword`` 등 민감 속성(SENSITIVE_ATTRIBUTES) 요청=AD 열거(BloodHound·ldapsearch)·Modify/Add/Del(primitive LDAPDN)/ModifyDN 쓰기=권한상승·백도어 계정·증거 정리·ExtendedRequest requestName OID(STARTTLS 1.3.6.1.4.1.1466.20037·PasswordModify) 단서, 노출된 389 디렉터리는 SMB/Kerberos AD 공격면 형제; filter 건너뛰고 base/scope/attributes 만·잘리면 None·offset 으로 한 세그먼트 다중 메시지 지원) 코어.
- :mod:`forensiclab.kerberos` — Kerberos KDC 메시지 파싱(RFC 4120, DER/ASN.1; :mod:`forensiclab.ldap`·:mod:`forensiclab.snmp` 와 TLV 골격 공유·관용 포트 88; LDAP(389)·Kerberos(88)·SMB 는 AD 공격면 형제 — LDAP 가 디렉터리 질의/인증이면 Kerberos 는 **티켓 발급(인증) 그 자체**; 최외곽 ``[APPLICATION n] SEQUENCE`` 로 메시지 자동 감지(10 AS-REQ·11 AS-REP·12 TGS-REQ·13 TGS-REP·14 AP-REQ·15 AP-REP·30 KRB-ERROR), 미지정 APP 태그/비-SEQUENCE 거부 오탐 방지, EXPLICIT context 태그 재귀 디코딩; **ASREProast**: ``PA-ENC-TIMESTAMP``(padata 타입 2) 사전인증 없는 AS-REQ(``is_asreproast_attempt``)=DONT_REQUIRE_PREAUTH 계정 AS-REP enc-part 오프라인 크래킹 표적·**Kerberoasting**: krbtgt 아닌 서비스 SPN(``sname``)을 RC4-HMAC(etype 23)으로 요청하는 TGS-REQ(``is_kerberoast_request``)·TGS-REP 발급 티켓 enc-part etype 이 RC4 면 실제 크래킹 가능(``is_kerberoastable_reply``, ticket_etype 추출)·etype 목록의 DES(1/3)·RC4(23/24)=암호 다운그레이드(``weak_etypes``)·KRB-ERROR ``error-code`` 6(C_PRINCIPAL_UNKNOWN)=사용자 열거·24(PREAUTH_FAILED)=패스워드 스프레이/브루트포스(LDAP resultCode 49·RADIUS Access-Reject 대응)·25(PREAUTH_REQUIRED)=정상·``cname``(user@REALM)/``sname``(SPN)/``realm`` 로 흐름→AD 계정·서비스 귀속 단서; PrincipalName ``comp1/comp2`` 결합·TCP 4바이트 길이 접두사는 offset=4 로 건너뜀·잘리면 None·offset 지원) 코어.
- :mod:`forensiclab.smb` — SMB1/SMB2 메시지 파싱(MS-SMB2, 순수 stdlib; LDAP(389)·Kerberos(88) 에 이은 **세 번째 AD 공격면 형제**·관용 포트 445 직접 SMB/139 NetBIOS — LDAP 가 디렉터리 질의, Kerberos 가 티켓 발급이면 SMB 는 파일/관리 공유 접근·원격 명령 실행(PsExec)·NTLM 인증 그 자체라 측면 이동 분석의 핵심; 직접 SMB 4바이트 전송 헤더(``0x00``+3바이트 길이) 자동 감지·제거 후 첫 4바이트 매직으로 버전 판별 — ``\\xffSMB``=**SMB1**(레거시·EternalBlue/MS17-010·WannaCry 무대, 협상 자체가 정황) / ``\\xfeSMB``=SMB2/3 64바이트 little-endian 헤더(Command·Flags(SERVER_TO_REDIR=응답)·Status·MessageId·SessionId·TreeId), 알려진 Command 검증 오탐 방지; **NEGOTIATE**: 제시 다이얼렉트 목록(0x0202~0x0311·0x02FF SMB1 폴백)·구버전(``offers_old_dialects``) 다운그레이드 정황·SecurityMode SIGNING_REQUIRED 비트 없으면 서명 미강제=**NTLM 릴레이**(ntlmrelayx) 가능 구성(``signing_not_required``)·**SESSION_SETUP**: SecurityBufferOffset 가리키는 GSS-API 블롭(없으면 전체)에서 ``NTLMSSP\\x00`` 시그니처 탐색 후 NTLMSSP 파싱 — AUTHENTICATE(타입 3)의 도메인/사용자/워크스테이션을 UNICODE/OEM 플래그 따라 평문 추출해 ``DOMAIN\\user`` 귀속(Kerberos cname·LDAP bind DN 대응)·**TREE_CONNECT**: 공유 경로(``\\\\host\\share``) UTF-16LE 추출·ADMIN$/C$/IPC$ 관리 공유(``is_admin_share``)=PsExec·원격 서비스 제어·측면 이동 표적, 노출된 445 는 RDP/SSH/VNC 형제 원격 접속 표적; 가변필드 offset 범위 검사·비정상 DialectCount 상한·잘리면 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.dcerpc` — DCE/RPC(MSRPC) PDU 파싱(DCE 1.1·MS-RPCE; LDAP(389)·Kerberos(88)·SMB(445) 에 이은 **다섯 번째 AD 공격면 형제** — SMB ``IPC$`` named pipe(``\\PIPE\\``) 또는 TCP 135 엔드포인트 매퍼/동적 고포트 위에서 실제 호출되는 원격 관리 인터페이스 그 자체, 측면 이동·권한 상승의 손잡이; 16바이트 공통 헤더(rpc_vers=5 검증·v4 connectionless 거부·알려진 ptype 만 인정 오탐 방지)에서 ``packed_drep`` 첫 옥텟 상위 4비트로 정수 엔디안(0 big/1 little, MS 는 대개 0x10) 판별해 frag/auth_length·call_id·UUID 디코딩에 반영; 핵심 단서 **BIND/ALTER_CONTEXT 의 abstract syntax 인터페이스 UUID** — p_context_elem 목록(n_context_elem×p_cont_elem)에서 컨텍스트별 UUID(앞 3필드 엔디안+뒤 8바이트 배열)·버전·transfer syntax(NDR/NDR64) 추출 후 KNOWN_INTERFACES 매핑으로 의도 식별: **DRSUAPI**(e3514235-)=DRSGetNCChanges(opnum 3) **DCSync**·**SVCCTL**(367abb81-)=원격 서비스 **PsExec**·**ATSVC/ITaskSchedulerService**=schtasks 원격 실행·**SAMR**/**LSARPC**=계정·SID 열거(BloodHound·lsadump)·**SPOOLSS**(MS-RPRN)=**PrintNightmare**(CVE-2021-34527)·**EFSRPC**(MS-EFSR)=**PetitPotam** 인증 강제·**WINREG**=SAM/SECURITY 하이브 원격 덤프·**EPM**(e1af8308-)=동적 포트 정찰; REQUEST 는 context_id+opnum(DRSUAPI opnum 3=DCSync 호출 상관)·BIND_NAK 는 거부 사유 추출·``bound_interfaces`` 편의 목록; 컨텍스트 목록 잘리면 읽은 만큼만·offset 지원) 코어.
- :mod:`forensiclab.ntlm` — NTLMSSP 메시지 파싱(MS-NLMP; 전송 무관 자격증명 인증 메커니즘 — SMB SESSION_SETUP·DCE/RPC auth verifier·HTTP ``Authorization: NTLM``·LDAP 어디에나 같은 ``NTLMSSP\\x00`` 으로 실린다. :mod:`forensiclab.smb` 가 SESSION_SETUP 안에서 domain/user 만 가볍게 뽑았다면 이 모듈은 NTLMSSP 메시지 **자체**를 독립 해석 — Kerberos 가 막히거나 다운그레이드된 환경의 실제 인증 흔적; 3-메시지 챌린지-응답: **타입1 NEGOTIATE**(flags 제시)·**타입2 CHALLENGE**(서버 8바이트 챌린지 nonce·TargetName·TargetInfo AV pair 목록[nb/dns 도메인·컴퓨터명])·**타입3 AUTHENTICATE**(domain\\user\\workstation 평문·LM/NT 챌린지 응답); 시그니처·MessageType(1/2/3) 검증 오탐 방지·메시지 타입별 NegotiateFlags 위치 차이 반영·UNICODE 비트로 UTF-16LE/OEM 인코딩 판별; **Net-NTLMv2 해시 추출**(:func:`netntlmv2`): CHALLENGE 서버 챌린지 + AUTHENTICATE NtChallengeResponse(NTProofStr[16]+blob)를 합쳐 hashcat ``-m 5600`` ``user::domain:srvchal:ntproof:blob`` 오프라인 크래킹 문자열 산출(Responder/ntlmrelayx 가 노리는 산출물)·**NTLMv1 다운그레이드** 판별(NT 응답 24바이트=v1, ``-m 5500`` DES 표적)·**서명/봉인 미협상**(SIGN/SEAL 비트 부재=릴레이 방어 약함, SMB signing 대응)·**널 세션**(user 비고 NT 응답 없음)·account 를 ``DOMAIN\\user`` 로 귀속(Kerberos cname·LDAP bind DN·SMB ntlm_account 대응); SPNEGO/GSS-API·HTTP 헤더 래핑은 :func:`find_ntlm` 으로 시그니처 스캔·가변필드 범위검사·AV pair 폭주 가드·offset 지원) 코어.
- :mod:`forensiclab.cldap` — CLDAP(비연결형 LDAP) 파싱(RFC 1798/3352·MS-ADTS; :mod:`forensiclab.ldap` 와 **같은 LDAP 와이어 포맷(BER/ASN.1·같은 protocolOp 태그)을 UDP 389** 에 얹은 변종 — 구조 파싱은 :func:`forensiclab.ldap.parse_ldap` 에 위임(코드 재사용)하고 UDP/CLDAP 에서만 의미가 생기는 단서를 그 위에 얹는다, :mod:`forensiclab.ldap`·:mod:`forensiclab.kerberos`·:mod:`forensiclab.smb` AD 공격면 형제; 연결·bind 없이 단일 searchRequest/searchResultEntry 한 방 경량 질의(MS-ADTS §6.3.3 LDAP ping); **반사·증폭 DDoS**: ``baseObject=""``+``scope=base`` rootDSE 질의(``is_rootdse_query``)는 작은 UDP 요청 한 방에 rootDSE 속성 전체를 토하게 만들어 증폭비 ~50–70×(:mod:`forensiclab.ntp` mode6/7·:mod:`forensiclab.snmp`·:mod:`forensiclab.ssdp`·:mod:`forensiclab.memcached` UDP 반사 계열)·속성 미지정/``*``/``+`` 와일드카드 일반 rootDSE 탐침을 ``is_amplification_probe`` 로 식별; **Netlogon DC 로케이터(LDAP ping)**: ``Netlogon`` 속성 요청(``is_netlogon_query``)으로 DC 탐색(nltest·Coercer·정찰), :mod:`forensiclab.ldap` 가 건너뛰는 **필터의 equalityMatch 를 직접 파내** ``DnsDomain``(``dns_domain``)·``Host``(``queried_host``)·``NtVer`` 대상 도메인/호스트 노출(and/or/not 재귀·깊이 상한)·**DC/디렉터리 정찰**: ``defaultNamingContext``·``dnsHostName``·``supportedSASLMechanisms`` 등 토폴로지 속성(``recon_attributes``); searchResultEntry/Done 응답측 식별(``is_search_response``, 반사 트래픽 응답)·offset 지원) 코어.
- :mod:`forensiclab.wsd` — WS-Discovery(WS-Dynamic Discovery) 파싱(SOAP 1.2 over UDP 3702·멀티캐스트 239.255.255.250:3702/FF02::C; :mod:`forensiclab.ssdp`(UPnP/HTTP-유사)·:mod:`forensiclab.mdns`·:mod:`forensiclab.llmnr`·:mod:`forensiclab.nbns` 와 **멀티캐스트 탐색 형제**이자, 동시에 **UDP 반사·증폭 DDoS 벡터 형제**(:mod:`forensiclab.ntp`·:mod:`forensiclab.snmp`·:mod:`forensiclab.ssdp`·:mod:`forensiclab.memcached`·:mod:`forensiclab.cldap`) — SSDP 가 HTTP-유사 평문이면 WSD 는 XML(SOAP) 멀티캐스트로 Windows 장치 탐색·ONVIF IP 카메라·네트워크 프린터·NAS 가 같은 표면; ``xml.etree.ElementTree`` 로 SOAP ``Envelope`` 파싱(localname 기준 비교로 네임스페이스 판 차이 흡수)·``DOCTYPE``/``ENTITY`` 선언 거부(billion-laughs 엔티티 폭탄 방어 + 정상 WSD 엔 DTD 없으므로 이상 정황)·비-SOAP/비-WSD 입력 ``None``; ``wsa:Action`` localname(없으면 body 요소 localname)으로 메시지 종류 판별(``Hello``·``Bye``·``Probe``·``ProbeMatches``·``Resolve``·``ResolveMatches``); **증폭 반사 DDoS**: ``Types``·``Scopes`` 빈 와일드카드 ``Probe``(``is_amplification_probe``)는 *모든* 장치가 응답하게 만들어 작은 UDP 한 방을 큰 ``ProbeMatches`` 로 부풀린다 — 노출된 3702 는 알려진 최대급 반사 증폭 벡터(~수백 배, :mod:`forensiclab.ssdp` ``ssdp:all``·:mod:`forensiclab.cldap` rootDSE 질의 계열)·**자산/장치 정찰**: ``Probe`` 의 ``Types``(ONVIF ``dn:NetworkVideoTransmitter``·프린터 ``PrintDeviceType``)가 표적 종류, ``Hello``/``ProbeMatch``/``ResolveMatch`` 의 ``XAddrs``(``device_addresses``)가 장치 서비스 URL(IP·포트) 노출(:mod:`forensiclab.ssdp` ``LOCATION`` 대응)·``Scopes``(``onvif://``·MAC·하드웨어) 핑거프린트·**상관**: ``EndpointReference`` ``Address``(``urn:uuid:``)·``MessageID``/``RelatesTo`` 로 질의-응답·장치 추적(:mod:`forensiclab.timeline`·:mod:`forensiclab.flows`)·위조 ``Hello`` 가짜 장치 유인 단서; ProbeMatch 다중 누적·offset 지원) 코어.
- :mod:`forensiclab.stun` — STUN(NAT 세션 통과 유틸리티) 파싱(RFC 5389/8489, UDP 3478·TLS 5349; :mod:`forensiclab.netdissect` 가 식별한 UDP 페이로드의 20바이트 고정 헤더+TLV 속성을 해석 — :mod:`forensiclab.ntp`(123)·:mod:`forensiclab.dns`(53)와 같은 위치, WebRTC·VoIP(SIP)·게임 P2P·TURN 릴레이의 토대; 상위 2비트 0+매직 쿠키 ``0x2112A442`` 검증으로 고전 RFC 3489·비-STUN 오탐 가드(고전 STUN 엔 쿠키 없음→``None``)·Message Type 14비트의 class(2비트)·method(12비트) **비트 교직 인코딩**(0x0100·0x0010 자리가 class)을 풀어 request/indication/success/error·Binding/Allocate 식별; **WebRTC IP 누출/de-anonymization**: ``XOR-MAPPED-ADDRESS``(매직 쿠키로 port^0x2112·addr^cookie 역XOR, IPv6 은 쿠키+txid 키)는 NAT/방화벽 바깥에서 본 단말 **반사 주소** — 브라우저 WebRTC 가 VPN·프록시 뒤에서도 진짜 공인 IP·포트를 흘려 ``mapped_address`` 로 호스트 귀속·익명화 우회 단서(없으면 평문 ``MAPPED-ADDRESS`` 폴백); **UDP 반사·증폭 DDoS 벡터 형제**(:mod:`forensiclab.ntp`·:mod:`forensiclab.snmp`·:mod:`forensiclab.ssdp`·:mod:`forensiclab.memcached`·:mod:`forensiclab.cldap`·:mod:`forensiclab.wsd`): 위조 소스로 노출 서버에 ``Binding`` 요청(``is_amplification_request``)을 퍼부으면 응답이 피해자에 반사·증폭; **TURN 릴레이 터널링**: ``Allocate``/``Send``/``Data`` 등 TURN 메서드(``is_turn``)·``XOR-RELAYED-ADDRESS``(``relayed_address``)는 릴레이 우회 채널 — C2·유출이 STUN/TURN 으로 위장 가능; **핑거프린트/상관**: ``SOFTWARE``(브라우저·VoIP·멀웨어 구현/버전)·``USERNAME``/``REALM``(자격증명)·12바이트 Transaction ID 로 질의-응답 상관(:mod:`forensiclab.timeline`·:mod:`forensiclab.flows`); TLV 4바이트 패딩·잘린 속성 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.mdns` — mDNS(Multicast DNS) 파싱(RFC 6762, UDP 5353; :mod:`forensiclab.dns` 와 **같은 와이어 포맷**·:mod:`forensiclab.llmnr`(5355)·:mod:`forensiclab.nbns`(137)와 **링크-로컬 이름 해석 삼형제** — 셋 다 멀티캐스트로 흘리며 같은 포이즈닝 표면 공유, 다만 mDNS 는 ``.local`` 네임스페이스+**DNS-SD 서비스 디스커버리**(RFC 6763)라 단서 결이 다름; 12바이트 DNS 헤더+표준 라벨 이름·Opcode 0 만 정의(비-0 거부 오탐 가드)·응답은 AA(권한) 비트; **자산/장치 정찰**: ``_services._dns-sd._udp.local`` 메타-질의 한 방이면 링크의 모든 서비스 타입 노출(``has_service_enumeration``)·PTR 질의/응답 서비스 타입(``_airplay._tcp``·``_googlecast._tcp``·``_ipp._tcp``·``_ssh._tcp``·``_smb._tcp``)이 장치 종류 핑거프린트(``queried_service_types``/``advertised_service_types``·:func:`service_label`); **mDNS 스푸핑/포이즈닝**(Responder mDNS 모드·Inveigh)이 ``.local`` 이름을 자기 IP 로 답해 NTLM 가로채기 — A/AAAA ``address``(``answer_addresses``)가 공격자 주장 목적지(:mod:`forensiclab.smb` NTLMSSP 상관, LLMNR 과 동일 메커니즘)·**QU(unicast-response) 비트**(질문 클래스 최상위 비트=유니캐스트 응답 요구, 포이즈너 응답경쟁 회피 ``unicast_requested``)·**cache-flush 비트**(응답 RR 클래스 최상위=캐시 강제 덮어쓰기 ``cache_flush``); 타입별 RDATA 해석 — A/AAAA IP·PTR/SRV 대상 이름(압축 포인터 추적)·SRV 포트·TXT key=value 목록; 압축 포인터 무한루프 가드·질문/답변 잘리면 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.sip` — SIP(Session Initiation Protocol) 파싱(RFC 3261, UDP/TCP 5060·TLS 5061; VoIP/영상통화의 **신호(signaling)** 프로토콜로 :mod:`forensiclab.stun`/TURN 의 **미디어(media)** 경로와 짝 — INVITE 의 SDP 바디 후보 주소가 STUN 반사 주소와 상관; 와이어 포맷이 :mod:`forensiclab.http` 와 매우 닮아(요청 라인+헤더+빈 줄+바디) 줄 단위 파싱을 그대로 빌려 SIP 단서만 위에 얹는다; 요청 라인 ``METHOD URI SIP/2.0``(:data:`SIP_METHODS` 화이트리스트+버전 ``SIP/2.0`` 정확 일치로 HTTP·비-SIP 오탐 가드)·상태 라인 ``SIP/2.0 CODE reason``·압축 헤더(``i``=Call-ID·``f``=From·``t``=To·``v``=Via·``m``=Contact 등)를 정식 이름으로 정규화해 호출 측이 예측 가능하게; **VoIP 스캐닝·toll fraud**: sipvicious ``friendly-scanner``·``sipcli``·``sundayddr`` 등 알려진 스캐너 User-Agent(``is_scanner``)로 내선·게이트웨이 탐색→무단 국제전화 단서·``REGISTER``/``OPTIONS``/``INVITE`` 무차별; **자격증명 수집·brute force**: ``Authorization``/``Proxy-Authorization: Digest`` 에서 ``username``(``auth_username``)·``realm``(``auth_realm``) 평문 추출(다이제스트 해시 크래킹·계정 열거, :mod:`forensiclab.ntlm` 자격증명 흔적 형제)·응답 401/407(``is_auth_required``) 인증 챌린지 진행; **통화 메타데이터·CDR 재구성**: ``From``/``To`` URI(누가 누구에게)·``Call-ID``(통화 상관)·``CSeq`` 로 INVITE→ACK→BYE 흐름(:mod:`forensiclab.timeline`·:mod:`forensiclab.flows`)·**발신번호 위조**: ``From`` 과 ``Via``/``Contact`` 불일치 caller-ID 스푸핑 정황·**핑거프린트**: ``User-Agent``/``Server`` IP-PBX(Asterisk·FreeSWITCH)·소프트폰·봇 식별; 바디 미해석 ``body_offset`` 만 노출·부분 수신 시 받은 데까지·offset 지원) 코어.
- :mod:`forensiclab.rtp` — RTP(실시간 전송 프로토콜) 파싱(RFC 3550/3551, 동적 UDP 짝수 포트; :mod:`forensiclab.sip`(신호)·:mod:`forensiclab.stun`/TURN(NAT 통과·미디어 경로 설정)이 차린 통로 위로 흐르는 **미디어(media)** 스트림 그 자체 — SIP INVITE 의 SDP 가 협상한 코덱·포트, STUN 반사 주소의 목적지가 RTP; 12바이트 고정 헤더 V(2)/P/X/CC·M/PT·seq·timestamp·SSRC 해석, 버전 정확히 2 검증으로 비-RTP 오탐 가드, RTCP(제어, PT 200–204) 같은 포트 다중화는 둘째 바이트로 가려(:func:`is_rtcp_packet`, RFC 5761) ``None``; **미디어 스트림 식별·통화 재구성**: ``SSRC`` 는 송신원 스트림 고유 ID — :mod:`forensiclab.sip` ``Call-ID``·:mod:`forensiclab.stun` Transaction ID 와 상관해 통화 당사자·시점을 잇고(:mod:`forensiclab.flows`·:mod:`forensiclab.timeline`)·``CSRC`` 목록(``has_contributors``)은 믹서를 거친 회의 참가자; **코덱·미디어 종류 핑거프린트**: ``payload_type`` 으로 음성(PCMU 0·PCMA 8·G729 18 ``is_audio``)·영상(H.263 34 ``is_video``)·동적 협상(96–127 ``is_dynamic_payload``) 구분 — G.711(0/8) 평문 음성은 통화 내용 자체 복원 표면(감청·증거); **RTP 인젝션·블리드**: 같은 SSRC 로 더 큰 ``sequence``/``timestamp`` 패킷 끼워넣기(injection)·오라우팅 RTP 엿듣기(bleed) — SSRC 충돌·시퀀스 점프 정황; **은닉 채널·스테가노그래피**: 헤더 확장(``X`` 비트·``ext_profile``/``ext_data``)·패딩·비정상 payload type 에 숨긴 C2·유출(:mod:`forensiclab.stun` TURN 터널링 결); CSRC/확장 길이 버퍼 초과 시 None·페이로드는 ``payload_offset`` 만 노출·offset 지원) 코어.
- :mod:`forensiclab.rtcp` — RTCP(RTP 제어 프로토콜) 파싱(RFC 3550 §6; :mod:`forensiclab.rtp`(미디어) 옆을 흐르는 **제어·통계** 채널 — 보통 RTP 다음 홀수 포트나 RFC 5761 로 같은 포트 다중화(:func:`forensiclab.rtp.is_rtcp_packet` 가 RTP 파서에서 가려 ``None`` 으로 떨군 패킷이 여기 대상), VoIP 삼각형 sip(신호)→sdp(협상)→rtp(미디어)에 RTCP 를 더해 통화 품질·당사자 신원·종료 시점까지 그림 완성; 공통 4바이트 헤더 V(2)/P/RC·SC(5)·PT(8)·length(워드-1) 해석, 버전 2 + PT 200–204 검증으로 비-RTCP 오탐 가드·length 가 버퍼 넘으면 ``None``; **호스트·신원 귀속(SDES CNAME)**: SDES(202) 청크의 ``CNAME``(보통 ``user@host``·:attr:`cnames`)은 :mod:`forensiclab.rtp` 의 익명 ``SSRC`` 를 사람·호스트에 못 박는 **신원 누출** — :mod:`forensiclab.sip` ``From`` URI·:mod:`forensiclab.flows` 와 상관해 통화 당사자 확정, ``TOOL``/``NAME``/``EMAIL``/``LOC``/``PHONE`` 는 소프트폰·사용자 핑거프린트; **통화 타이밍·시계 편차(SR NTP)**: 발신자 보고(SR 200)의 64비트 NTP 타임스탬프(:attr:`ntp_epoch` 로 Unix epoch 환산)는 송신원 벽시계 — 캡처 시각 대비 편차로 호스트 식별·로그 보정·통화 시작 상관(:mod:`forensiclab.timeline`); **통화 품질·경로(RR)**: 수신자 보고 블록 누적 손실(24비트 부호, 음수=중복)·지터·LSR/DLSR 왕복 지연으로 품질 저하·경로 변경 정황; **통화 종료(BYE 203)**: BYE SSRC 목록·선택적 reason 텍스트로 세션 끝 시점·사유(정상 vs 강제) 표시; **은닉 채널(APP 204)**: 앱 정의 name 4바이트+임의 데이터는 미디어로 위장한 C2·유출 운반 표면(:mod:`forensiclab.rtp` 확장·:mod:`forensiclab.stun` TURN 터널링 결); :func:`parse_rtcp` 단일·:func:`parse_rtcp_compound` 복합(SR/RR+SDES 연결) 패킷 리스트·중간 모순 시 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.zrtp` — ZRTP 미디어 키 합의(in-band) 파싱(RFC 6189; VoIP 삼각형 sip(신호)→sdp(협상)→rtp(미디어)→rtcp(제어)에 더해 **미디어 암호 키를 어떻게 합의하느냐**를 RTP 와 같은 짝수 UDP 포트에서 직접 처리 — :mod:`forensiclab.sdp` ``a=crypto``(SDES)가 세션 키를 신호 평문에 그대로 실어 신호 경로를 본 사람이 미디어를 복호할 수 있던 것과 **정반대로**, ZRTP 는 키를 신호에 절대 싣지 않고 미디어 경로에서 Diffie-Hellman 으로 합의(Zfone·Phil Zimmermann) — ZRTP 가 보이면 SDP 만으로 미디어 복호 불가·수동 감청 차단의 강한 신호; 와이어 포맷은 RTP 와 닮았으나 헤더 둘째 워드 **매직 쿠키 ``ZRTP``(0x5a525450)** 로 구분(:mod:`forensiclab.rtp` 가 버전 2 아니라 떨군 짝수 포트 패킷·:func:`forensiclab.rtp.is_rtcp_packet` 결의 다중화 가드 형제, 비-ZRTP 면 ``None``); 12바이트 헤더(0x10 프리앰블+seq·매직·SSRC) 뒤 메시지(프리앰블 ``0x505a``+length 워드+8바이트 ASCII **Message Type Block** ``Hello``/``Commit``/``DHPart1``/``Confirm1``/``GoClear``/``Error`` 등+본문+CRC); **지속 신원·호스트 귀속(ZID)**: Hello 의 12바이트 ``zid`` 는 단말이 통화마다 재사용하는 **영구 식별자** — :mod:`forensiclab.rtp` 익명 SSRC·바뀌는 IP 를 넘어 같은 장치를 통화·세션 간 못 박는 슈퍼쿠키(:mod:`forensiclab.flows`·:mod:`forensiclab.timeline` 상관·:mod:`forensiclab.rtcp` SDES CNAME 신원 누출 결); **소프트폰 핑거프린트**: Hello 16바이트 ``client_id``(``Zfone``·``GNU ZRTP``·``Linphone``·``CSipSimple``)는 구현·버전(:mod:`forensiclab.sip` User-Agent 결); **암호 자세**: Hello hash/cipher/auth/key-agreement/SAS 알고리즘 목록·Commit 선택값으로 협상 강도(약한 DH 그룹·구식 알고리즘 정황); **평문 다운그레이드 공격**: ``GoClear``/``ClearACK``(``is_clear_downgrade``)는 암호화 미디어를 평문으로 되돌리는 전환 — 감청용 MITM 강제 다운그레이드 정황; **협상 실패·MITM**: ``Error`` ``error_code`` 버전/알고리즘 불일치(SAS 음성 확인은 별도); :func:`parse_zrtp` 단일·:func:`looks_like_zrtp` 다중화 가드·헤더만 있고 본문 잘리면 헤더 필드까지·offset 지원) 코어.
- :mod:`forensiclab.sdp` — SDP(Session Description Protocol) 파싱(RFC 4566/8866; :mod:`forensiclab.sip` 가 ``body_offset`` 만 알려 주던 INVITE/200 바디가 바로 SDP — VoIP 삼각형 sip(신호)→**sdp(세션 협상)**→:mod:`forensiclab.rtp`(미디어)의 가운데 꼭짓점으로, "어떤 코덱을·어느 IP·포트로·평문이냐 암호화냐"를 적은 평문 텍스트; ``<type>=<value>`` 한 글자 타입 줄 묶음을 줄 단위로 파싱하되 ``m=`` 줄 이후는 해당 **미디어 블록** 소속으로 묶고, 첫 의미 줄이 ``v=<숫자>`` 가 아니면 비-SDP 오탐 가드로 ``None``; **미디어 IP 누출/de-anonymization**: ``c=`` 연결 주소(``connection_address``·``media_addresses``)·``a=candidate`` ICE 후보(``all_candidates``)는 미디어가 실제 흐르는 host/srflx/relay IP — VPN/프록시 뒤 사설·공인 주소 평문 노출로 :mod:`forensiclab.stun` XOR-MAPPED 반사 주소와 상관(호스트 귀속); **코덱·포트 협상**: ``m=`` 미디어 줄(종류·포트·전송·payload type 목록)·``a=rtpmap``(``rtpmap`` dict, 정적 표 0=PCMU/8=PCMA 보조)은 :mod:`forensiclab.rtp` 가 볼 SSRC 스트림의 포트·코덱 예고 — G.711 평문 음성은 통화 내용 복원 표면; **암호화 자세**: 전송 ``RTP/AVP`` 면 평문 RTP(``has_cleartext_media``·감청), ``RTP/SAVP``/``UDP/TLS/RTP/SAVP`` 면 SRTP(``is_encrypted``)·``a=crypto`` SDES 는 **세션 키를 SDP 평문에 그대로 실어**(``all_crypto``) 신호 경로를 본 사람이 미디어 복호 가능한 치명 노출·``a=fingerprint`` DTLS-SRTP 핑거프린트; **세션 핑거프린트·상관**: ``o=`` origin username(``origin_username``)·세션 ID·발신 IP(``origin_address``)·``s=`` 세션명으로 통화 당사자·도구 귀속(:mod:`forensiclab.sip` Call-ID·:mod:`forensiclab.timeline` 과 연결); 미디어 방향(``direction`` sendrecv/sendonly/recvonly/inactive)·플래그 속성·망가진 줄 건너뜀·str/bytes 모두 수용) 코어.
- :mod:`forensiclab.socks` — SOCKS 프록시 핸드셰이크 파싱(SOCKS4/4a·SOCKS5 RFC 1928; :mod:`forensiclab.netdissect` 가 식별한 TCP 페이로드 위에서 한 호스트가 임의 목적지로의 연결을 **대신 중계**하도록 요청하는 협상 — :mod:`forensiclab.http` CONNECT 프록시가 평문 헤더라면 SOCKS 는 바이트 지향 바이너리라 별도 해석, 침해 분석의 **피벗·터널링 손잡이**(proxychains·Tor·SSH -D·멀웨어 내장 프록시); **목적지 귀속**: 요청 ``dst_host``/``dst_port`` 는 터널 종착지로 :mod:`forensiclab.flows` 의 프록시까지 흐름 너머 진짜 C2·측면 이동 표적, SOCKS4a·SOCKS5 ``ATYP=domain`` 은 목적지를 **도메인 그대로** 실어(``is_hostname_target``) :mod:`forensiclab.dns` 질의가 없어도 C2 도메인 노출(원격 DNS); **무인증 열린 프록시**: SOCKS5 그리팅 제공 메서드에 ``NO_AUTH``(``offers_no_auth``)=누구나 쓰는 열린 중계·``USERNAME_PASSWORD``(``offers_userpass``)=평문 자격증명 예고; **사용자 귀속**: SOCKS4 NUL 종단 ``userid``(:mod:`forensiclab.ident` 결, 위조 가능); **유출 채널**: SOCKS5 ``UDP_ASSOCIATE``(``is_udp_associate``)=UDP 릴레이 DNS 터널·대역외 유출(:mod:`forensiclab.stun` TURN 결); 와이어 — SOCKS4 ``VN=4``·CD·DSTPORT·DSTIP·USERID(0.0.0.x=SOCKS4a 도메인 동봉)·응답 ``VN=0``·SOCKS5 그리팅(VER·NMETHODS·메서드)·요청(VER·CMD·RSV·ATYP·ADDR·PORT); 05 로 시작하는 그리팅/요청 모호성은 RSV=0+유효 CMD/ATYP+해당 ATYP 최소 길이 충족 시에만 요청 판정해 짧은 그리팅 오판 방지·잘리면 가용분까지·offset 지원) 코어.
- :mod:`forensiclab.ike` — IKE/ISAKMP 키 교환 핸드셰이크 파싱(IKEv1 RFC 2408/2409·IKEv2 RFC 7296, UDP 500·4500; :mod:`forensiclab.netdissect` 가 식별한 UDP 페이로드 위에서 두 호스트가 **IPsec VPN 터널의 키를 협상**하는 ISAKMP 핸드셰이크 — :mod:`forensiclab.socks`(응용 계층 프록시 터널)·:mod:`forensiclab.stun`(NAT 통과)에 이은 **네트워크 계층 암호 터널(IPsec)의 손잡이**, 터널 자체는 암호화되지만 협상 헤더·일부 페이로드는 평문; 28바이트 ISAKMP 헤더(Initiator/Responder SPI 8+8·Next Payload·Version MjVer<<4|MnVer·Exchange Type·Flags·Message ID·Length) + 4바이트 제네릭 페이로드 헤더로 연결된 페이로드 체인 순회, 버전 1/2·교환 타입 유효·Initiator SPI 비-0 검증으로 비-IKE 오탐 가드(``None``); **공격자 표적 Aggressive Mode(IKEv1 ET=4)**: 메인 모드(2)가 신원을 암호로 보호하는 것과 달리 어그레시브 모드는 **신원(ID)·PSK 인증 해시를 평문**으로 주고받아 캡처만으로 ``ike-scan``/``psk-crack`` 오프라인 사전 공격 가능(``is_aggressive_mode``)·레거시 VPN 게이트웨이 대표 약점; **VPN 종단·세션 귀속**: 8바이트 ``initiator_spi``/``responder_spi`` SA 쿠키로 :mod:`forensiclab.flows` 양방향 흐름을 한 협상에 못 박고·Responder SPI 전부 0=첫 IKE_SA_INIT(``is_initial``) 협상 시작 시점; **구현 핑거프린트**: VID 페이로드 해시(``vendor_ids``)로 구현·기능 식별(Cisco·strongSwan·NAT-T·DPD); **버전·협상 결과**: IKEv1/IKEv2 구분(``is_ikev1``/``is_ikev2``)·Initiator/Response 플래그(``is_initiator``/``is_response``)·Notify 메시지 타입(``notify_types``)으로 ``NO_PROPOSAL_CHOSEN``·``AUTHENTICATION_FAILED`` 실패/브루트포스 정황; **NAT-T 마커**: UDP 4500 의 4바이트 0 비-ESP 마커 자동 감지(``has_non_esp_marker``)=NAT 뒤 VPN 클라이언트; IKEv1 암호화 플래그·IKEv2 SK(46) 페이로드 이후는 암호문이라 풀지 않음·페이로드 체인 잘림/모순(길이 0·버퍼 초과·무한 루프) 시 받은 데까지·offset 지원) 코어.
- :mod:`forensiclab.gre` — GRE(Generic Routing Encapsulation) 터널 헤더 파싱(RFC 2784/2890·RFC 2637, IP 프로토콜 47; :mod:`forensiclab.netdissect` 가 IPv4 헤더에서 식별한 proto 47 페이로드 위에서 한 패킷을 다른 패킷 안에 감싸 나르는 **터널 캡슐** 헤더 — :mod:`forensiclab.ike` 가 IPsec 암호 터널의 *키 협상* 손잡이라면 GRE 는 TCP/UDP 가 아닌 **L3 위 범용 터널 운반층** 그 자체로 헤더가 평문이라 안에 무엇을(``protocol_type``)·어느 세션인지(``key``/Call ID)를 그대로 드러냄; 가변 헤더 Flags+Version(2바이트 C/R/K/S/s/Recur·A/Flags/Ver)·Protocol Type(2) 뒤 C 면 Checksum(2)+Reserved1(2)·K 면 Key(4)(버전1 은 Payload Length(2)+Call ID(2))·S 면 Sequence(4)·A(버전1) 면 Ack(4), 버전 0/1 만 인정·버전1 은 protocol PPP(0x880B) 강제로 비-GRE 오탐 가드(``None``); **방화벽 우회·은닉 터널**: GRE 는 포트 없는 IP 프로토콜이라 포트 필터를 비껴가 내부 IP 패킷을 감싸 외부로 빼면(IP-in-IP ``carries_ipv4``/``carries_ipv6``) C2·대량 유출을 평범한 터널로 위장 — 내부 ``protocol_type`` 으로 캡슐 안 종류 식별; **PPTP VPN 데이터 평면**: 버전1+PPP(``is_pptp``)는 MS-CHAPv2·MPPE 약점으로 깨지는 레거시 PPTP — 보이는 것 자체가 위험 신호·16비트 **Call ID**(``call_id``)는 세션 식별자로 :mod:`forensiclab.flows` 흐름을 한 세션에 못 박음(``sequence``/``ack`` 세션 진행); **ERSPAN 원격 포트 미러링(감시·캡처)**: protocol 0x88BE(II)·0x22EB(III)(``is_erspan``)는 스위치가 다른 포트 트래픽 전체를 떠서 원격 수집기로 보내는 채널 — 비인가 ERSPAN 은 곧 네트워크 감청·증거 수집 표면; **NVGRE L2-over-L3 터널**: 0x6558 Transparent Ethernet Bridging(``is_nvgre``)은 이더넷 프레임 통째 캡슐화(VXLAN 형제·L3 경계 넘는 은닉 브리지); **터널 상관·세션 귀속**: 32비트 ``key`` 로 같은 터널 양방향 묶음(keepalive·다중 터널 구분); 캡슐 안쪽은 풀지 않고 ``payload_offset`` 만 노출(:mod:`forensiclab.netdissect` 재귀 해석 대상)·옵션 필드 잘리면 ``None``·offset 지원) 코어.
- :mod:`forensiclab.geneve` — GENEVE(Generic Network Virtualization Encapsulation) 오버레이 터널 헤더 파싱(RFC 8926, UDP 6081; :mod:`forensiclab.netdissect` 가 식별한 UDP 페이로드 위에서 프레임 하나를 통째로 UDP/IP 안에 감싸 나르는 L2/L3-over-L3 오버레이 터널 — :mod:`forensiclab.vxlan`(UDP 4789 고정 8바이트)·:mod:`forensiclab.gre` NVGRE(IP proto 47) 와 같은 가상화 캡슐 형제이나, GENEVE 는 둘의 한계를 푼 **확장형**: VXLAN 이 고정 8바이트라 메타데이터를 못 싣는 데 반해 가변 길이 **TLV 옵션**을 헤더에 달 수 있고 VXLAN(표준)이 내부를 항상 이더넷으로 가정하는 것과 달리 **Protocol Type** 으로 내부 종류를 직접 명시(VMware NSX-T 기본 오버레이); 기본 헤더 8바이트 Ver(2비트)·Opt Len(6비트 4바이트 단위)·O(OAM)·C(Critical 옵션)·Rsvd·Protocol Type(2)·VNI(24비트)·Reserved(1) 뒤 Opt Len×4 바이트 옵션 TLV(Option Class 2·Type 1 최상위=critical·R+Length 5비트 4바이트 단위), 버전 0 만 인정·옵션 영역이 버퍼 넘으면 비-GENEVE 오탐 가드(``None``); **은닉 L2/L3 브리지·세그먼트 경계 우회**: 비인가 VTEP 은 라우팅·방화벽 분리망을 평범한 UDP 6081 로 위장해 통째 연결 — 내부 프레임(``payload_offset``) 재해석하면 ARP·내부 IP·평문 노출(VXLAN 와 동일 표면 다른 포트·포맷); **세그먼트 귀속(VNI)**: 24비트 ``vni`` 로 가상 네트워크(테넌트) 못 박아 :mod:`forensiclab.flows` 같은 UDP 흐름 안에서도 오버레이 구분(VXLAN vni·NVGRE key 대응); **내부 종류 직접 노출**: ``protocol_type`` 0x6558 TEB=이더넷(``carries_ethernet``)·0x0800 IPv4(``carries_ipv4``, IP-in-UDP 은닉 터널)·0x86DD IPv6(``carries_ipv6``) 헤더만으로 식별; **TLV 옵션 메타데이터·은닉 채널**: ``option_class``+``type``+데이터 묶음(NSX 논리 포트·보안 그룹 등), Critical 비트(C)인데 모르는 옵션이면 패킷을 버려야 하므로 알 수 없는 critical 옵션(``has_critical_option``)은 비표준 구현·은닉 채널·메타데이터 주입 정황; **터널 종단 식별**: 바깥 UDP/IP 출발/목적 IP 가 곧 VTEP 쌍(:mod:`forensiclab.netdissect`/:mod:`forensiclab.flows` 와 묶어 누가 브리지를 거는지 확정); 캡슐 안쪽은 풀지 않고 ``payload_offset`` 만 노출(재귀 해석 대상)·옵션 TLV 잘리면 읽은 데까지·8바이트 미만 None·offset 지원) 코어.
- :mod:`forensiclab.vxlan` — VXLAN(Virtual eXtensible LAN) 오버레이 터널 헤더 파싱(RFC 7348·RFC 8926, UDP 4789/8472; :mod:`forensiclab.netdissect` 가 식별한 UDP 페이로드 위에서 이더넷 프레임 하나를 통째로 UDP/IP 안에 감싸 나르는 **L2-over-L3 오버레이 터널** 헤더 — :mod:`forensiclab.gre` 의 NVGRE(0x6558 TEB)가 IP 프로토콜 47 위에서 같은 일을 한다면 VXLAN 은 **UDP 위**에서 처리해 포트 기반 장비를 그대로 통과, 헤더 평문이라 어느 세그먼트(``vni``)를 어떤 종류로(GPE ``next_protocol``) 잇는지 드러냄; 고정 8바이트 헤더 Flags(1)·Reserved(3)·VNI(3)·Reserved(1), 표준은 **I 비트(0x08)** 만 켜지고 나머지 0·GPE 는 **P 비트(0x04)** + 마지막 바이트 Next Protocol, I 비트 없거나 표준인데 마지막 Reserved 바이트≠0 이면 비-VXLAN 오탐 가드(``None``); **은닉 L2 브리지·세그먼트 경계 우회**: 비인가 VTEP 은 라우팅·방화벽으로 분리한 망을 평범한 UDP 4789 로 위장해 이더넷 수준에서 통째 연결 — 내부 프레임(``payload_offset``) 재해석하면 ARP·내부 IP·평문 프로토콜 노출; **세그먼트 귀속(VNI)**: 24비트 ``vni`` 로 어느 가상 L2 세그먼트(테넌트)인지 못 박아 :mod:`forensiclab.flows` 같은 UDP 흐름 안에서도 오버레이 구분(NVGRE ``key``/VSID 대응); **VXLAN-GPE 임의 프로토콜 직접 운반**: P 비트(``is_gpe``)면 이더넷 아닌 IPv4/IPv6/NSH/MPLS 직접 적재(``next_protocol``·``carries_ipv4``/``carries_ipv6``/``carries_ethernet``) IP-in-UDP 은닉 터널·서비스 체이닝(NSH) 정황; **터널 종단 식별**: 바깥 UDP/IP 의 출발/목적 IP 가 곧 VTEP 쌍(:mod:`forensiclab.netdissect`/:mod:`forensiclab.flows` 와 묶어 누가 브리지를 거는지 확정); 캡슐 안쪽(이너 이더넷/IP)은 풀지 않고 ``payload_offset`` 만 노출(재귀 해석 대상)·8바이트 미만 ``None``·offset 지원) 코어.
- :mod:`forensiclab.esp` — ESP(Encapsulating Security Payload) IPsec 데이터 평면 헤더 파싱(RFC 4303, IP 프로토콜 50·NAT-T 시 UDP 4500; :mod:`forensiclab.ike` 가 IPsec VPN 터널의 **키를 협상**하는 손잡이였다면 ESP 는 그 협상 산물인 SA 로 암호화된 실제 사용자 트래픽이 흐르는 **데이터 평면** 그 자체 — 평문으로 읽히는 건 앞 8바이트 SPI(4)+Sequence(4)뿐이고 페이로드·Padding·Pad Length·Next Header·ICV 는 전부 암호문이라 풀 수 없음, **풀 수 없다는 사실 자체가 암호 VPN 데이터 정황**; :mod:`forensiclab.gre` 가 평문 헤더로 안에 무엇이 들었는지 그대로 드러내는 범용 터널이었던 것과 정반대; **SA 귀속·터널 상관(SPI)**: 32비트 ``spi`` 는 수신자가 복호 SA 를 고르는 단방향 색인이라 양방향 터널은 SPI 두 개 — :mod:`forensiclab.flows` 같은 IP 쌍 흐름 안에서도 어느 SA(어느 IKE 협상 산물)인지 못 박고 rekey 시 바뀌는 SPI 로 SA 수명 추적(IKE ``initiator_spi``/``responder_spi`` SA 쿠키와는 별개 값); **흐름 진행·재생 공격(Sequence)**: 32비트 ``sequence`` 는 SA 마다 1부터 단조 증가하는 anti-replay 카운터 — ``sequence==1``(``is_initial``)=SA 첫 패킷(터널 개통 직후)·큰 값=장수명/대량 전송·같은 SPI 에서 되감김/중복=재생 공격·캡처 재주입 정황; **NAT-T 데이터 평면**: NAT 뒤 VPN 은 ESP 를 UDP 4500 으로 캡슐화(ESP-in-UDP) — 첫 4바이트 0 인 비-ESP 마커(:mod:`forensiclab.ike` ``has_non_esp_marker``)는 IKE, 0 아니면 ESP, **SPI 0 은 예약값이라 거부**해 IKE 비-ESP 마커를 ESP 로 오인하지 않음; 암호화된 본문은 풀지 않고 ``payload_offset`` 만 노출·8바이트 미만 또는 SPI 0 이면 ``None``·offset 지원) 코어.
- :mod:`forensiclab.l2tp` — L2TP(Layer 2 Tunneling Protocol) 헤더 파싱(RFC 2661, L2TPv2; UDP 1701; :mod:`forensiclab.ike` 키 협상·:mod:`forensiclab.esp` 암호 데이터 평면과 **한 묶음으로 배치되는** 또 다른 터널 운반층 — Windows/iOS 기본 "L2TP/IPsec" 의 그 L2TP. **L2TP 자체는 암호화를 전혀 제공하지 않고**(RFC 2661 §9.4) PPP 프레임을 UDP 1701 위로 나르기만 함, 기밀성은 전적으로 바깥 IPsec ESP 에 의존 — 그래서 ESP 로 안 감싸인 **평문 L2TP 가 1701 에 보이면** 보호되지 않은 VPN 설정 오류, 안의 PPP·CHAP·자격증명이 그대로 캡처되는 직접 증거; **제어 vs 데이터 평면(T 비트)**: 제어(``is_control``, T=1; SCCRQ/SCCRP/ICRQ 등 터널·세션 협상)는 본문이 AVP 라 호스트명·벤더·인증 평문, 데이터(``is_data``, T=0)는 PPP 프레임 운반; **터널·세션 귀속**: 16비트 ``tunnel_id``·``session_id`` 로 :mod:`forensiclab.flows` 같은 IP 쌍 안에서도 어느 터널·세션인지 못 박아 한 터널 안 다중 PPP 세션 분리, ``tunnel_id==0``(``is_setup``)=ID 배정 전 최초 협상(SCCRQ); **제어 채널 진행·재전송(Ns/Nr)**: 16비트 ``ns``(이 메시지 시퀀스)·``nr``(다음 기대)로 신뢰 전송 추적, 같은 Ns 재출현=재전송·Nr 점프=손실; 가변 헤더 Flags+Version(T·L·S·O·P + 하위 4비트 Version=2)·L 비트면 Length·Tunnel/Session ID·S 비트면 Ns/Nr·O 비트면 Offset Size+Pad, Version≠2 또는 제어인데 L/S 비트 빠짐(RFC §3.1 강제) 또는 헤더 잘림이면 오탐 가드 ``None``; PPP/AVP 페이로드는 풀지 않고 ``payload_offset`` 만 노출·offset 지원) 코어.
- :mod:`forensiclab.wireguard` — WireGuard 메시지 헤더 파싱(WireGuard 프로토콜; UDP 기본 51820; :mod:`forensiclab.ike`·:mod:`forensiclab.esp`·:mod:`forensiclab.l2tp` 가 레거시/표준 IPsec VPN 묶음이라면 WireGuard 는 그 자리를 대체하는 **현대 VPN** — Linux 커널 내장·모바일 기본 앱. ESP 처럼 핸드셰이크 산물 키로 ChaCha20-Poly1305 암호화하지만 프레임 구조가 완전히 다름: 1바이트 type + **반드시 0 인 3바이트 reserved** + little-endian 평문 색인/카운터; 결정적 식별 특징은 **메시지별 고정 길이**(Initiation 148B·Response 92B·Cookie 64B)라 그 자체가 강한 핑거프린트—**비표준 포트로 위장해도 식별**, reserved=0 과 합쳐 임의 UDP 오인 확률 최소화; **현대 VPN 존재·비표준 포트 위장**: 고정 길이는 포트 무관, 풀 수 없다는 사실 자체가 암호 VPN·방화벽 우회 은닉 터널 정황; **세션 귀속·터널 상관**: 32비트 ``sender_index``·``receiver_index`` 는 ESP SPI 대응, 각 끝이 자기 색인 통보하고 :mod:`forensiclab.flows` 같은 IP 쌍 안에서 양방향 한 세션으로 못 박음, 핸드셰이크 재수행 시 색인 변경=rekey 수명 추적; **전송 진행·재생 공격**: 64비트 ``counter`` 세션마다 0부터 단조 증가 nonce/anti-replay, ``counter==0``(``is_initial_transport``)=핸드셰이크 직후 첫 전송(세션 개통)·큰 값=장수명/대량·되감김/중복=재생 공격(ESP ``sequence`` 대응); **과부하·DoS 완화**: 핸드셰이크 mac2≠0(``mac2_present``)=응답자 과부하 cookie 챌린지, Cookie Reply(type 3) 출현과 짝지어 핸드셰이크 플러딩 탐지; type 1~4 아님·reserved≠0·고정 길이 미달(전송 최소 32B)이면 오탐 가드 ``None``; 암호 본문(ephemeral/encrypted/mac)은 풀지 않고 ``payload_offset`` 만 노출·offset 지원) 코어.
- :mod:`forensiclab.openvpn` — OpenVPN 패킷 헤더 파싱(OpenVPN 프로토콜; UDP/TCP 기본 1194; :mod:`forensiclab.wireguard` 가 IPsec 묶음을 대체하는 현대 커널 VPN 이라면 OpenVPN 은 **가장 널리 쓰이는 사용자공간 TLS VPN** — 상용 VPN·기업 원격접속의 사실상 표준. WireGuard 가 고정 길이로 강하게 식별되는 것과 달리 OpenVPN 은 TLS 제어 채널(핸드셰이크)과 암호 데이터 채널을 같은 소켓에 다중화하며 **UDP(1194)·TCP(흔히 443 위장) 모두** 탐—TCP/443 위장은 방화벽·DPI 우회 대표 수법; 첫 바이트 **opcode(상위 5비트)+key_id(하위 3비트)**, 제어/리셋/ACK 패킷은 뒤에 **8바이트 평문 Session ID**(ESP SPI·WireGuard sender_index 대응 세션 귀속 핸들), 데이터 패킷은 opcode 뒤 전부 암호문(P_DATA_V2 만 3바이트 peer-id 평문); tls-auth/tls-crypt 켜지면 HMAC+packet-id 가 끼어들어 ACK 배열 위치가 설정 의존이라 **설정 없이 확실한 부분(opcode·key_id·session_id·peer_id)만** 풀고 나머지는 ``payload_offset`` 만 노출; **TLS VPN 존재·포트 위장**: opcode 유효(1~11)+8B 세션 ID 면 OpenVPN, 1194 아닌 TCP/443·UDP/53 위장해도 핸드셰이크 구조로 식별·풀 수 없는 데이터 채널 자체가 암호 터널 정황; **세션 귀속·터널 상관**: 8바이트 ``session_id`` 각 끝이 통보, :mod:`forensiclab.flows` 같은 IP 쌍 양방향 한 세션으로 못 박음·hard reset 시 새 세션 ID 로 수명 추적; **세션 개통·방향**: ``is_hard_reset``(opcode 1·2·7·8·10)=TLS 핸드셰이크 시작, ``is_client_reset``(1·7·10)/``is_server_reset``(2·8)=개시자/응답자 방향·``is_soft_reset``(3)=rekey; **다중 클라이언트 서버**: ``peer_id``(P_DATA_V2 24비트)=서버가 한 소켓에서 여러 클라이언트 구분; **레거시 핑거프린트**: hard reset V1(1·2)=구버전·V2/V3(7·8·10)=현대 클라이언트·key_id 회전=rekey 진행; TCP 전송 시 2바이트 길이 접두사는 ``offset=2`` 로 건너뜀; opcode 1~11 아님·제어인데 세션 ID 8B 미달·P_DATA_V2 인데 peer-id 3B 미달이면 오탐 가드 ``None``; 암호 본문·HMAC·ACK 배열은 풀지 않고 ``payload_offset`` 만 노출·offset 지원) 코어.
- :mod:`forensiclab.pptp` — PPTP 제어 채널 메시지 파싱(RFC 2637; TCP 1723; :mod:`forensiclab.l2tp`·:mod:`forensiclab.openvpn`·:mod:`forensiclab.wireguard` 와 같은 VPN 터널 묶음의 한 형제이자 **가장 오래된 대중 VPN — Microsoft PPTP**(Windows 내장). 제어와 데이터가 두 채널로 갈림: **제어 채널은 TCP 1723 위 평문**(본 모듈), **데이터 채널은 Enhanced GRE**(proto 47, :mod:`forensiclab.gre` 가 GRE Key=Call ID 노출); MS-CHAPv2+MPPE 의 구조적 약점(chapcrack/cloudcracker 로 사실상 완전 해독, NIST 폐기 권고)으로 **레거시·약한 VPN 의 대표** — 1723 에 제어 메시지가 보이는 것 자체가 폐기 권고된 약한 VPN 사용 정황; 모든 메시지가 **12바이트 공통 헤더**(Length<2>+Message Type<2: 1=Control·2=Management>+**Magic Cookie ``0x1A2B3C4D`` 고정 상수**+Control Message Type<2: 1~15>+Reserved0<2>)로 시작, Magic Cookie 는 WireGuard 고정 길이처럼 **결정적 핑거프린트**라 포트 위장에도 식별·임의 TCP 오인 차단; 이후 **확실한 평문 부분만** 추출: **SCCRQ(1)/SCCRP(2)** 64바이트 ``hostname``+64바이트 ``vendor_string``(NUL 패딩) 평문 단말 호스트명·구현 벤더(Microsoft·linux) 강한 핑거프린트(L2TP SCCRQ AVP 계열)+``protocol_version``·SCCRP 는 ``result_code``/``error_code``; **Outgoing/Incoming-Call-Request(7·9)** ``call_id`` — 발신측 16비트 호출 식별자로 **데이터 채널 GRE 의 Key 필드에 그대로 실려**(:mod:`forensiclab.gre` 와 상관해 제어 협상↔암호 데이터 터널을 한 호출로 못 박음); **Call-Reply(8·10)** ``call_id``+``peer_call_id``(양방향 GRE 두 Call ID 쌍)+``result_code``/``error_code``; **ICCN(11)/Disconnect(13)/WAN-Error(14)** ``peer_call_id`` 로 호출 수명·종료 추적; **레거시 VPN 존재**(Magic Cookie+타입 1~15=PPTP 정황·포트 위장 식별·1723 자체가 폐기 권고 위반)·**단말 식별**(평문 hostname/vendor)·**제어↔데이터 상관**(call_id↔GRE Key, peer_call_id 쌍 양방향 :mod:`forensiclab.flows`)·**호출 수명·실패**(result/error·teardown) 단서; 12B 미달·Magic Cookie 불일치·Message Type≠1·2·Control Type∉1~15 면 오탐 가드 ``None``, 메시지별 필드는 바이트 모자라면 해당 필드만 ``None`` 부분 파싱·한 세그먼트 다중 메시지 ``length``/``offset`` 이어 읽기) 코어.
- :mod:`forensiclab.gtp` — GTP(GPRS Tunneling Protocol) 헤더 파싱(3GPP TS 29.060 GTPv1·TS 29.274 GTPv2-C; UDP 2123 GTP-C·2152 GTP-U; :mod:`forensiclab.gre`·:mod:`forensiclab.l2tp`·:mod:`forensiclab.esp`·:mod:`forensiclab.wireguard` 와 같은 터널 운반층 묶음의 한 형제이나 그것들이 기업·개인 VPN 인 데 비해 **이동통신 코어망(GPRS/UMTS/LTE/5G NSA)의 내부 터널** — 단말(UE) 트래픽을 기지국↔게이트웨이(SGSN/GGSN·S-GW/P-GW) 사이에서 캡슐화. 두 평면: **GTP-C(제어, 2123)** 세션(PDP/EPS 베어러) 생성·수정·삭제 시그널링·**GTP-U(사용자, 2152)** 단말 IP 패킷을 ``G-PDU``(255)로 캡슐화하는 데이터 평면으로 :mod:`forensiclab.gre` 처럼 **안의 IP 패킷이 평문**이라 헤더만 벗기면 단말 트래픽 그대로 복원; GTP-U 출현 자체가 **코어망 내부 트래픽**(로밍·합법감청 LI·운영자 백홀·노출된 코어 장비) 강한 정황·비표준 포트 GTP 는 GTP-in-GTP·코어망 침투·시그널링 폭주 정찰 단서; GTPv1 헤더(최소 8B): 1바이트 Flags(Version<상위 3비트=1>·PT<1=GTP·0=GTP'>·예약0·E/S/PN 플래그)+Message Type+2B Length<8B 헤더 이후 길이>+**4바이트 ``teid``**(Tunnel Endpoint Identifier — :mod:`forensiclab.esp` SPI·:mod:`forensiclab.pptp` Call ID·:mod:`forensiclab.wireguard` sender_index 대응 **세션 귀속 키**로 같은 IP 쌍 안 어느 베어러/세션인지 못 박고 양방향 터널 상관·세션 재생성 시 바뀌는 TEID 로 수명 추적), E/S/PN 중 하나라도 켜지면 선택 4B(Sequence 2+N-PDU 1+Next Ext Header Type 1); GTPv2-C 헤더(LTE/EPC; 최소 8B): Flags(Version=2·P·T)+Message Type+2B Length<처음 4B 이후>+T=1 이면 4B TEID·이어 3B Sequence+1B 예약; **평면·메시지 식별**(``is_user_data`` G-PDU=데이터·``is_session_create``/``is_session_delete`` Create/Delete PDP·Session=터널 개통/종료·``is_error_indication`` 알 수 없는 TEID 수신=터널 불일치·``is_echo`` 경로 생존)·**세션 귀속·터널 상관**(teid+sequence :mod:`forensiclab.flows`)·**캡슐 단말 트래픽 복원**(G-PDU ``payload_offset`` 이 평문 단말 IP, :mod:`forensiclab.gre` 형제) 단서; 본문 IE TLV 는 풀지 않고 ``payload_offset``(고정 헤더 끝=본문/캡슐 IP 시작, gre/esp 동일 의미; 가변 확장 헤더 제외)만 노출; 8B 미만·Version∉1·2·GTPv1 인데 PT≠1 또는 예약≠0·Message Type 미정의면 오탐 가드 ``None``, 선택 필드는 바이트 모자라면 해당 필드만 ``None``·offset 지원) 코어.
- :mod:`forensiclab.quic` — QUIC 롱 헤더 파싱(RFC 9000 QUIC v1·RFC 9369 QUIC v2; UDP 흔히 443; :mod:`forensiclab.wireguard`·:mod:`forensiclab.openvpn`·:mod:`forensiclab.esp` 가 암호 VPN 터널 묶음이라면 QUIC 는 그 사촌격 **현대 암호 전송 계층** — HTTP/3 가 그 위에서 돌고 구글/유튜브/CDN 트래픽의 큰 몫. TCP+TLS 를 UDP 하나로 합쳐 거의 모든 것을 암호화하나 **롱 헤더(핸드셰이크 구간)** 는 버전과 두 Connection ID 를 평문으로 노출; 첫 바이트 최상위 비트(``0x80`` Header Form)로 **롱 헤더**(Initial/0-RTT/Handshake/Retry·Version Negotiation; 버전·DCID·SCID 평문—본 파서 표적)와 **숏 헤더**(1-RTT 데이터; DCID 에 길이 필드 없어 상태 없이 못 떼어냄·헤더 보호로 첫 바이트 하위 비트도 암호화) 가름, 숏 헤더는 ``None`` 반환하고 :func:`~forensiclab.quic.is_short_header` 로 식별만; 와이어(롱, big-endian) 첫 바이트(Form 1·Fixed 1·Type 2비트·보호 4비트)+4B Version+1B DCID Length+DCID+1B SCID Length+SCID, Version==0 이면 Version Negotiation(이후 4B 지원 버전 목록); **현대 암호 전송 존재·포트 위장**(풀 수 없는 QUIC 가 443 아닌 임의 UDP 포트=암호 터널/우회 정황)·**연결 상관·이주 추적**(``dcid``/``scid`` 각 끝이 자기 Connection ID 통보, :mod:`forensiclab.flows` IP 쌍을 넘어 **NAT 재바인딩·connection migration** 후에도 같은 DCID 면 동일 세션으로 못 박음—WireGuard sender/receiver index·ESP SPI 대응)·**클라이언트 핑거프린트**(32비트 ``version`` 이 구현/세대 가름: ``quic_v1``·``quic_v2``·``draft-NN`` 구형·``grease`` ``0x?a?a?a?a`` 버전 협상 강제 더미=TLS GREASE 처럼 라이브러리 핑거프린트)·**핸드셰이크 단계·DoS 완화**(``is_initial`` 핸드셰이크 개시·``is_retry`` 응답자 주소검증/과부하 완화=WireGuard cookie·SYN cookie 대응·``is_version_negotiation`` 서버가 클라 버전 미지원 통보·Initial 폭주/Retry 출현=핸드셰이크 플러딩/증폭 정찰) 단서; 와이어 2비트 패킷 종류 코드는 v1·v2 가 뒤섞여 버전별 매핑으로 추상 종류(initial/0rtt/handshake/retry)로 정규화; 롱 헤더 아님(숏 헤더 포함)·Connection ID 길이>20·버전 있는데 Fixed 비트(``0x40``)=0(Version Negotiation 은 임의값이라 미검사)·바이트 부족이면 오탐 가드 ``None``; 토큰·패킷 번호·암호 본문은 풀지 않고 ``payload_offset``(SCID 직후)만 노출·offset 지원) 코어.
- :mod:`forensiclab.m3ua` — M3UA(MTP3 User Adaptation Layer) 공통 헤더·파라미터 파싱(RFC 4666·SCTP 2905; :mod:`forensiclab.sctp` 가 "SS7 의 IP 후신 SIGTRAN M3UA(2905) 가 그 위에 실린다"고 할 때 그 **M3UA 가 바로 이 적응 계층** — 전통 전화 신호망 SS7 의 하위 MTP1/2(물리·링크)를 IP+:mod:`forensiclab.sctp` 로 갈고 망 라우팅 계층 MTP3 만 적응해 IP 위로 올린 것이라 그 위에 SCCP·ISUP·TCAP 실제 SS7 사용자부가 실림; :mod:`forensiclab.diameter`(LTE/EPC) 와 **세대만 다른 같은 코어망 신호 평면**으로 둘 다 :mod:`forensiclab.sctp` 위에 얹힘; :mod:`forensiclab.sctp`·:mod:`forensiclab.diameter`·:mod:`forensiclab.gtp` 처럼 **8바이트 공통 헤더만** 풀고 파라미터는 값 무파싱·태그/길이 헤더만 훑어 열거하고 ``payload_offset``·``value_offset`` 으로만 가리킴; 와이어(big-endian) version<1: 항상 1, 아니면 오탐 가드>+reserved<1: 0 고정>+message_class<1: 0 MGMT·1 TFER 실제 SS7 데이터·2 SSNM 신호망 관리·3 ASPSM ASP 상태·4 ASPTM ASP 트래픽·9 RKM 라우팅 키>+message_type<1: 클래스 안 세부—class+type 이 메시지 못 박음>+message_length<4: 공통 헤더 포함 전체>, 이어서 파라미터들 parameter_tag<2>+parameter_length<2: 헤더 4 포함, 4바이트 경계 패딩>; **신호 평면 식별**(SCTP 2905 위 M3UA 노출 자체가 통신사 코어망 SS7/SIGTRAN 정황·diameter 3868 과 짝지어 4G 전후 신호망 추적)·**링크 수립 흐름**(``is_aspsm``/``is_asptm`` ASPUP→ASPUP-ACK→ASPAC→ASPAC-ACK→BEAT 워치독 :mod:`forensiclab.timeline`·ASPUP 폭주=비인가 ASP 연결 시도 코어망 침투)·**SS7 데이터 운반**(``is_data`` class 1 Payload Data 에 ``has_protocol_data`` Protocol Data 0x0210 파라미터=실제 SS7 라우팅 레이블 OPC/DPC+SCCP/ISUP=가입자 위치추적·SMS 가로채기·과금 사기 SS7 공격 와이어 증거)·**망 가용성 조작**(``is_ssnm`` DUNA 도달 불가·DAVA 도달 가능·SCON 혼잡 폭주=신호점 가용성 위조 라우팅 교란)·**오류·통보**(``is_error`` ERR·``is_notify`` NTFY 빈발=거부/상태 변화) 단서; ``class_name``/``message_name``+``param_names``/``has_param`` 로 열거; 8B 미만·version≠1·reserved≠0·message_length<8 이면 오탐 가드 ``None``·파라미터 0개 허용(M3UA 는 본문 없는 메시지 존재)·절단 캡처면 담을 수 있는 파라미터까지만·파라미터 값 무파싱·offset 지원) 코어.
- :mod:`forensiclab.sccp` — SCCP(Signalling Connection Control Part) 파싱(ITU-T Q.713; SS7 사용자부; :mod:`forensiclab.m3ua` 가 "Protocol Data 파라미터(0x0210) 안에 SS7 라우팅 레이블(OPC/DPC)+**SCCP/ISUP** 페이로드가 그대로 들어 있다"고 할 때 그 **SCCP 가 바로 이 계층** — M3UA/MTP3 가 신호점 사이 *망 라우팅*만 한다면 SCCP 는 그 위에서 노드 안 어떤 응용(subsystem)에게 줄지(SSN)와 무엇보다 전화번호꼴 **Global Title(GT, E.164)로 주소 지정**해 라우팅하고 그 위에 TCAP→MAP 이동망 응용부가 실려 가입자 위치·인증·SMS 질의가 오감; :mod:`forensiclab.m3ua`·:mod:`forensiclab.sctp` 처럼 **연결 없는 데이터 메시지(UDT/UDTS/XUDT/XUDTS)** 고정부와 두 주소만 풀고 사용자 데이터(TCAP)는 무파싱·``data_offset`` 으로만 가리킴; 와이어(Q.713) message_type<1: 0x09 UDT·0x0A UDTS·0x11 XUDT·0x12 XUDTS 연결 없는 TCAP 운반·0x01 CR 등 연결 지향은 타입만>+protocol_class 또는 return_cause<1>+[hop_counter<1: XUDT/XUDTS>]+포인터들<각 1옥텟, 자기 위치 기준 상대 오프셋, 0=없음>, 가변부 각 주소는 length<1>+address_indicator<1: bit1 0x01 PC·bit2 0x02 SSN·bit3-6 GTI·bit7 0x40 Routing Indicator GT↔SSN>+[point_code<2: ITU 14비트 LE>]+[ssn<1>]+[global_title BCD 숫자]; **가입자 위치추적·SMS 가로채기**(MAP SRI-SM/SendRoutingInfo/PSL 질의가 Called Party GT 에 **피해자 MSISDN**(전화번호)을 실어 HLR(SSN 6)로 보냄·그 ``global_title_digits`` 가 곧 표적 번호)·**표적 노드 식별**(``ssn_name`` HLR 6·VLR 7·MSC 8·MAP 5·gsmSCF 147·GMLC 145 가 위치 vs 과금 vs SMS 의도 가름)·**출처 위장 SS7 스푸핑**(Calling Party GT/Point Code 가 통신사 경계 넘어온 비인가 노드면 망 침투·같은 GT 가 다수 번호 훑으면 정찰 :mod:`forensiclab.flows`·:mod:`forensiclab.timeline` 상관)·**라우팅/주소 오류**(``is_unitdata_service`` UDTS/XUDTS 반송 폭주=주소 변환 실패) 단서; ``message_type_name``/``is_connectionless``/``routing_indicator``+포인터·주소 절단이면 풀 수 있는 필드까지만·미정의 메시지 타입(0x01~0x14 밖)·빈 입력이면 오탐 가드 ``None``·TCAP 본문 무파싱·offset 지원) 코어.
- :mod:`forensiclab.tcap` — TCAP(Transaction Capabilities Application Part) 파싱(ITU-T Q.773; SS7 응용부; :mod:`forensiclab.sccp` 가 연결 없는 데이터 메시지(UDT/XUDT)의 ``data_offset`` 으로 "사용자 데이터(TCAP)는 여기서 시작"이라 가리켰을 때 그 **TCAP 가 바로 이 계층** — SCCP 가 SSN·전화번호(GT)로 *주소를 지정*해 메시지를 날랐다면 TCAP 는 그 위에서 **대화(transaction)를 묶고 원격 연산(operation)을 호출**하는 RPC 격 계층, 그 위에 MAP 이 실려 ``SendRoutingInfoForSM``·``AnyTimeInterrogation``·``SendAuthenticationInfo`` 같은 실제 SS7 공격 연산이 호출됨; :mod:`forensiclab.sccp`·:mod:`forensiclab.m3ua` 처럼 **BER/ASN.1 을 얕게만** 순회—메시지 타입 태그·트랜잭션 ID·dialogue/component 포션 경계만 풀고 MAP 인자 본문은 무파싱·``payload_offset`` 으로만 가리킴, 컴포넌트는 타입·invokeID·operationCode 까지만; 와이어(Q.773 BER, 모두 APPLICATION 클래스 구성형 태그) message_type_tag<1: 0x61 Unidirectional·0x62 Begin 대화 개시·0x64 End 종료·0x65 Continue 진행·0x67 Abort 비정상 종료>+length<BER>+트랜잭션 포션<0x48 Originating TID·0x49 Destination TID·0x4A P-Abort Cause·0x6B Dialogue Portion·0x6C Component Portion> 안에 컴포넌트들<0xA1 Invoke·0xA2 ReturnResultLast·0xA3 ReturnError·0xA4 Reject·0xA7 ReturnResultNotLast>; **연산 식별(operationCode)**(Invoke 의 ``operation_code``/``operation_name`` 이 곧 무엇을 하려는지—MAP 45 sendRoutingInfoForSM=SMS 가로채기용 표적 라우팅 조회·71 anyTimeInterrogation/83 provideSubscriberLocation=가입자 위치추적·56 sendAuthenticationInfo=인증 벡터 탈취·22 sendRoutingInfo=통화 가로채기, ``is_attack_operation``/``has_attack_operation``/``SS7_ATTACK_OPERATIONS`` 로 의도 분류, SCCP Called GT 표적 번호와 짝지으면 "누구를 무엇으로" 완성)·**대화 상관(Transaction ID)**(Begin 의 originating TID 와 그에 답하는 End/Continue 의 destination TID 가 한 대화를 :mod:`forensiclab.flows`·:mod:`forensiclab.timeline` 너머로 묶음, SCCP 주소·SCTP verification tag·ESP SPI 대응 상관 키)·**거부·오류**(``is_abort``+``p_abort_name`` unrecognizedTransactionID/resourceLimitation 폭주·ReturnError ``error_code``) 단서; ``message_type_name``/``is_begin``/``is_continue``+컴포넌트 ``component_name``/``is_invoke``·operationCode 가 OID(global)면 ``is_global_operation`` 만; 빈 입력·첫 태그가 Q.773 메시지 타입(0x61/0x62/0x64/0x65/0x67) 아님·길이 옥텟 절단이면 오탐 가드 ``None``·TLV 절단이면 풀 수 있는 만큼만·MAP 인자 무파싱·offset 지원) 코어.
- :mod:`forensiclab.isup` — ISUP(ISDN User Part) 파싱(ITU-T Q.763; SS7 전화 호 제어부; :mod:`forensiclab.m3ua` 가 "Protocol Data(0x0210) 안에 SS7 라우팅 레이블+**SCCP/ISUP**"라 할 때 :mod:`forensiclab.sccp`(→TCAP→MAP 이동망 질의)와 **나란히 갈라지는 다른 한 갈래** — SCCP 가 노드 안 응용을 주소지정하는 데이터/질의 평면이라면 ISUP 는 MTP3(M3UA) 위에 **직접 실려** 회선(trunk)마다 전화 호(call)를 세우고·울리고·이어주고·끊는 **호 제어 평면**, PSTN 과 IP 후신(SIGTRAN) 양쪽에서 통화의 골격; :mod:`forensiclab.sccp`·:mod:`forensiclab.tcap` 처럼 **CIC·메시지 타입은 모든 메시지에서** 풀고 포렌식 핵심인 **IAM 의 당사자 번호·REL 의 cause** 만 깊게 디코드, 그 외 본문은 무파싱·``payload_offset`` 으로만 가리킴; 와이어(Q.763, MTP3 라우팅 레이블 다음) CIC<2: little-endian 하위 12비트, 한 통화가 일어난 회선 상관 키>+message_type<1: 0x01 IAM 호 개시·0x06 ACM 착신 도달/링·0x09 ANM 통화 연결/과금 시작·0x0C REL 해제·0x10 RLC 해제 완료·0x14 BLO 회선 차단 등>+파라미터부<메시지별 mandatory fixed+mandatory variable(포인터 기반, 자기 위치 기준 상대 오프셋 0=없음, SCCP 와 동일 규약)+optional(TLV, 0x00 종단)>; **호 설정·당사자 번호(CDR 재구성)**(IAM 의 ``called_number`` 피호출 번호 필수·``calling_number`` 발신 번호 선택을 BCD 디코드—"발신→착신" 쌍이 통화 기록 핵심, SCCP 가 GT MSISDN 푼 것과 같은 깊이)·**발신번호 위조(caller ID spoofing)**(``calling_presentation``/``is_calling_number_restricted`` 제시 제한 표시 APRI 가 restricted=표시 억제·notAvailable=발신번호 미상 위조·익명 통화 정황)·**호 수명·종료 사유(타임라인)**(IAM→ACM→ANM→REL/RLC 가 한 호의 :mod:`forensiclab.timeline`·CIC 가 어느 회선인지 못 박는 상관 키 TCAP TID·SCTP verification tag·ESP SPI 대응·REL ``cause_value``/``cause_name`` Q.850 unallocatedNumber/userBusy/normalCallClearing 으로 끊긴 이유=통화 사기 탐침 없는 번호 대량 발신·플러딩 단서)·**호 흐름 식별**(``is_setup``/``is_address_complete``/``is_answer``/``is_release``/``is_release_complete``); CIC·타입 못 읽음(3옥텟 미만)·타입이 Q.763 정의에 없음·빈 입력이면 오탐 가드 ``None``·포인터/파라미터 절단이면 풀 수 있는 필드까지만·offset 지원) 코어.
- :mod:`forensiclab.map` — MAP(Mobile Application Part) 가입자 신원 추출 파싱(3GPP TS 29.002; SS7 응용; :mod:`forensiclab.tcap` 가 Invoke 컴포넌트의 ``operationCode`` 로 "무엇을 하려는지"(45 sendRoutingInfoForSM·71 anyTimeInterrogation·56 sendAuthenticationInfo …)를 드러냈을 때 **그 연산의 인자(argument)에 실리는 표적 가입자 신원(IMSI/MSISDN)이 바로 이 모듈이 푸는 대상** — TCAP 가 "무엇을"이라면 MAP 인자는 **"누구를"**, 둘을 짝지어야 SS7 공격 표적이 와이어에서 완성됨(:mod:`forensiclab.tcap` 의 "표적 번호와 짝지으면 누구를 무엇으로 완성"이 이 지점, SCCP Called GT MSISDN 과도 교차); MAP 은 :mod:`forensiclab.sccp`·:mod:`forensiclab.tcap` 처럼 고정 헤더 없이 연산별 깊게 중첩된 ASN.1 이라 **전체 디코드를 시도하지 않고** 포렌식상 가장 중요한 한 가지—**가입자 신원 식별자**—만 얕게·보수적으로 추출; 식별자는 두 *부호화 형태*로만 나타남: **AddressString(전화번호꼴)** 선두 1옥텟(ext|nature of address|numbering plan) 다음 TBCD 숫자—MSISDN·Service Centre 주소 E.164(``form="address"``)·**bare TBCD(맨 숫자열)** 선두 옥텟 없이 통째 TBCD—IMSI·IMEI E.212(``form="digits"``); 두 형태는 스키마 없이 완전 구분 불가(둘 다 OCTET STRING 베이스)라 **부호화 형태로만** 분류(스키마 추측 안 함)—선두 옥텟에 ext 비트(0x80) 서고 numbering plan 이 알려진 값(ISDN 1·E.212 6 등 :data:`~forensiclab.map._ADDRESS_PLANS`)이면 ``address``, 아니면 ``digits``, 결정적·검증 가능·과대해석 회피(실제 캡처 다수인 ``0x91`` international·ISDN 은 정확히 ``address``); :func:`~forensiclab.map.decode_tbcd`(옥텟당 2자리·하위 nibble 먼저·0xF filler·0xA~E=``*#abc``)·:func:`~forensiclab.map.decode_address_string`(선두 옥텟+TBCD→``(digits, nature, plan)``) 공개 헬퍼; BER/ASN.1 을 **얕게만** 순회—primitive OCTET STRING·context 태그만 신원 시도, 구성형(SEQUENCE·context constructed)은 ``_MAX_DEPTH`` 까지 재귀해 ``subscriberIdentity [0] CHOICE{imsi[0],msisdn[1]}`` 같은 한 겹 포장 통과, 그 외 인자 본문 무파싱; **표적 신원**(``target_digits`` 첫 신원=인자 선두 표적 식별자·``all_digits``·``identities`` 각 ``MapIdentity`` digits/form/``nature_name``/``numbering_plan_name``/offset)·**연산 의도 짝**(``operation_name``/``is_attack_operation`` 은 :mod:`forensiclab.tcap` 의 ``_MAP_OPERATION_NAMES``·``SS7_ATTACK_OPERATIONS`` 표 재사용으로 드리프트 차단—operationCode 없이도 신원 추출 동작)·**한계**(신원 라벨 MSISDN vs IMSI 를 스키마로 단정 안 하고 ``form`` 으로만 구분, IMSI 둘째 자리 8/9 라 선두 옥텟이 우연히 ext 비트 가지면 드물게 오분류 가능하나 plan nibble 검사로 대부분 걸러지고 숫자열 자체는 대체로 복원) 단서; 빈 입력·첫 TLV 파싱 불가면 오탐 가드 ``None``·후보가 유효 TBCD 아니거나 자릿수 비현실적(4~16 밖)이면 신원 미채택·절단되면 가용분까지·``end`` 로 인자 경계 제한·offset 지원) 코어.
- :mod:`forensiclab.cap` — CAP(CAMEL Application Part) 호 제어·과금 연산/번호 추출 파싱(3GPP TS 29.078; SS7 응용; :mod:`forensiclab.tcap` 가 Invoke 의 ``operationCode`` 로 "무엇을"을 드러낼 때 :mod:`forensiclab.map`(MAP, 이동성·SMS·인증) 과 **나란히 TCAP 위에 실리는 다른 응용부** — CAMEL 은 지능망(IN) 서비스(선불 과금·번호 변환·부가 통화 제어)를 위해 교환기 gsmSSF↔서비스 제어부 gsmSCF 사이에서 호 하나하나를 실시간 조종, MAP 이 "누구의 위치/SMS/키"(가입자 평면)였다면 CAP 은 **"이 통화를 어디로·과금을 어떻게"**(호 제어/과금 평면); 포렌식상 **CAMEL 사기**의 무대 — ``connect``(20)·``initiateCallAttempt``(32) gsmSCF 가 통화를 임의 번호로 되돌리거나 새 호 발신=통화 가로채기·프리미엄/리비뉴셰어 사기·``furnishChargingInformation``(34)·``applyCharging``(35)·``sendChargingInformation``(46) 과금 정보 조작=무료 통화·과금 우회·``releaseCall``(22) 강제 절단·``establishTemporaryConnection``(17)·``connectToResource``(19) 자원/안내 삽입; ``initialDP``(0, Initial Detection Point) 호 설정 시점 교환기→SCF 트리거에 **착신/발신 번호**(calledPartyNumber·callingPartyNumber)·위치·IMSI, ``connect``(20) 인자에 **재라우팅 대상 번호**(destinationRoutingAddress)가 실려 둘 다 얕게 추출; CAP 번호는 :mod:`forensiclab.map` 의 AddressString(E.164 선두 옥텟+TBCD)과 달리 **ISUP(Q.763) 형식**(:mod:`forensiclab.isup` 당사자 번호와 동일) octet1=odd/even(bit8)+nature of address(bits7-1)·octet2=numbering plan(bits7-5) 등·이어서 BCD 주소 신호라 MAP TBCD 아닌 ISUP 형 BCD 디코드(:func:`~forensiclab.cap.decode_isup_bcd`·:func:`~forensiclab.cap.decode_cap_number` 공개 헬퍼); :mod:`forensiclab.map`·:mod:`forensiclab.tcap` 처럼 BER/ASN.1 **얕게만** 순회—번호 후보(primitive OCTET STRING)만 ISUP 형으로 분류·디코드하고 그 외 본문 무파싱, 구성형은 ``_MAX_DEPTH`` 까지 재귀해 ``destinationRoutingAddress [0] SEQUENCE OF CalledPartyNumber`` 한 겹 포장 통과; **연산 식별·사기 의도**(``operation_name``/``is_fraud_operation`` 은 CAP 전용 :data:`~forensiclab.cap.CAP_OPERATION_NAMES`·:data:`~forensiclab.cap.CAP_FRAUD_OPERATIONS` — MAP 과 local 값이 겹쳐도 뜻이 다르므로(22 가 MAP 은 sendRoutingInfo·CAP 은 releaseCall) :mod:`forensiclab.tcap` MAP 표 미재사용·drift 차단, operationCode 없이도 번호 추출 동작)·**표적 번호**(``target_number`` 첫 번호=connect 재라우팅 대상·initialDP 착신·``all_digits``·``numbers`` 각 :class:`~forensiclab.cap.CapNumber` digits/``nature_name``/``numbering_plan_name``/``is_international``/offset, SCCP Called GT·ISUP called_number·MAP target_digits 와 교차 상관)·**한계**(ISDN-AddressString 형 필드 mscAddress·SMSC 주소는 미분류=ISUP 형 당사자 번호만 대상·번호 라벨 착신 vs 발신 vs 재라우팅을 스키마로 단정 안 하고 등장 순서로만 추정); 보수적 채택—nature of address·numbering plan 둘 다 알려진 ISUP 값이고 자릿수 현실적(3~15)일 때만 번호로 채택(스키마 추측 없음·과대해석 회피)·빈 입력·첫 TLV 파싱 불가면 오탐 가드 ``None``·절단되면 가용분까지·``end`` 경계 제한·offset 지원) 코어.
- :mod:`forensiclab.smstpdu` — SMS-TPDU(GSM 단문 전송 계층 PDU) 파싱(3GPP TS 23.040/23.038; :mod:`forensiclab.map` 이 ``mo-forwardSM``(44)·``mt-forwardSM``(46) 연산의 인자에서 **표적 가입자 신원(MSISDN/IMSI)**(``target_digits``)을 뽑았을 때 *바로 그 같은 인자에 함께 실려 있는 것*—**SMS 메시지 그 자체**(SM-RP-UI 필드의 SMS-TPDU)가 이 모듈의 대상; MAP 이 "누구의 SMS 인가"라면 SMS-TPDU 는 **"그 SMS 의 내용·발신자·시각"**, 즉 SS7 SMS 가로채기(:mod:`forensiclab.tcap` 45 sendRoutingInfoForSM→라우팅 확보→46 mt-forwardSM 주입)에서 *실제로 가로채지거나 위조되는 페이로드*—MAP target_digits 가 "누구를"이면 SMS-TPDU 는 "무슨 말을"; SMS-TPDU 는 SS7 의 BER/ASN.1 과 달리 **고정 위치 옥텟 스트림**(TS 23.040 §9.2)이라 :mod:`forensiclab.map`·:mod:`forensiclab.isup` 처럼 얕게 순회하지 않고 **메시지 타입(MTI)별 고정 필드를 순서대로** 푼다(스키마 추측 없음); 와이어 첫 옥텟 TP-MTI<bits1-0: 0 SMS-DELIVER 수신·1 SMS-SUBMIT 발신·2 STATUS-REPORT/COMMAND>+TP-UDHI<bit6: 사용자 데이터 헤더 존재>+TP-RP<bit7>+TP-SRI/SRR<bit5>, DELIVER 는 TP-OA(발신 주소)·PID·DCS·**TP-SCTS(7옥텟 반옥텟 스왑 BCD 서비스센터 수신 시각)**·UDL·UD, SUBMIT 은 TP-MR·TP-DA(착신 주소)·PID·DCS·TP-VP(유효기간, 값 무해석)·UDL·UD; **주소**(TP-OA/TP-DA: 길이+type-of-address 옥텟+swapped-BCD 반옥텟, :func:`~forensiclab.smstpdu.decode_sms_address`—:mod:`forensiclab.map` :func:`~forensiclab.map.decode_tbcd` 반옥텟 부호화 재사용 드리프트 차단·alphanumeric TON(5)이면 주소가 GSM7 팩이라 :func:`~forensiclab.smstpdu.decode_gsm7` 로 푸는 발신자명 스푸핑 표면)·**시각**(:func:`~forensiclab.smstpdu.decode_scts` SCTS→``"YY-MM-DD HH:MM:SS ±TZ"`` 메시지 :mod:`forensiclab.timeline` 의 못)·**본문**(TP-UD 를 TP-DCS 가 가리키는 부호로 디코드—GSM 7비트 기본 알파벳 septet 언팩(:func:`~forensiclab.smstpdu.decode_gsm7` 확장 이스케이프 0x1B 포함)·8비트·UCS2 UTF-16BE, 가로채진/위조된 텍스트 자체가 증거—피싱 링크·스미싱·OTP 탈취)·**표적·연결 조각**(``target_number`` 착신(submit)/발신(deliver)·``is_concatenated`` UDH IEI 0x00/0x08 로 긴 SMS 조각 여부·``user_data_header`` UDHI 면 UDH 분리하고 7비트 septet 정렬 패딩 보정); ``message_type``/``is_deliver``/``is_submit``·``payload_offset`` 으로 TP-UD 시작만 노출; 빈 입력·1옥텟 미만·offset 범위 밖이면 오탐 가드 ``None``·STATUS-REPORT/COMMAND(MTI 2/3)는 헤더 플래그까지만·절단되면 풀 수 있는 필드까지만 채우고 나머지 ``None``·읽기 전용 무변형·offset 지원) 코어.
- :mod:`forensiclab.smpp` — SMPP(Short Message Peer-to-Peer) 파싱(SMPP v3.4·TCP 2775; :mod:`forensiclab.smstpdu` 가 푼 SMS-TPDU 가 *이동망 신호 내부*(MAP forwardSM SM-RP-UI)의 SMS 와이어였다면 SMPP 는 그 SMS 가 **코어망 바깥 응용(ESME)에서 SMSC 로 드나드는 길목** — A2P 대량발송·OTP·마케팅·그리고 **스미싱 캠페인이 실제 주입되는 평면**; SMS-TPDU 가 SS7 BER/ASN.1·고정옥텟이라면 SMPP 는 TCP 위 **고정 16바이트 헤더+본문**(command_length·command_id·command_status·sequence_number, 응답은 command_id 에 0x80000000 OR)이라 :mod:`forensiclab.smstpdu` 처럼 본문 고정필드를 *순서대로* 풀되 구조 아는 PDU 만 깊게—bind_transmitter/receiver/transceiver 는 ``system_id``·**평문 ``password``**(C-octet string, :mod:`forensiclab.ftp`·:mod:`forensiclab.smtp` 평문 로그인급 노출)·system_type·interface_version 까지, submit_sm/deliver_sm 은 ``source_addr``(위조 발신자명/숏코드)·``destination_addr``(표적)·``data_coding``·``short_message``(UCS2 0x08 UTF-16BE 그 외 latin-1 무손실—스미싱 링크·OTP 텍스트 자체) 까지, 그 외(enquire_link 등)는 헤더와 ``payload_offset`` 으로만; **자격증명 노출·브루트포스**(bind system_id/password 누가 어느 SMSC 에 붙었나·응답 command_status ESME_RINVPASWD(0x0E)/ESME_RINVSYSID(0x0F) 반복=패스워드 추정 RADIUS Access-Reject 대응)·**당사자·본문**(SMS-TPDU TP-OA/TP-DA/TP-UD 대응이되 SMSC 진입 *직전* 평문이라 위조 발신자명 그대로)·**연산·상관·결과**(``command_name``/``is_request``/``is_response`` bind→enquire_link→submit_sm :mod:`forensiclab.timeline`·``sequence_number`` 요청↔응답 :mod:`forensiclab.flows` IP쌍 안 상관·``is_error``/``command_status_name`` ESME_* 오류) 단서; ``command_id`` 가 알려진 SMPP 연산 아님·command_length<16·16바이트 미만이면 TCP 스트림 오탐 가드 ``None``·본문 절단이면 가용분까지·command_name/status_name 공개헬퍼·읽기 전용 무변형·offset 지원) 코어.
- :mod:`forensiclab.ucp` — UCP/EMI(Universal Computer Protocol / External Machine Interface) 파싱(EMI/UCP 4.x·흔히 TCP 2024/3024; :mod:`forensiclab.smpp` 의 **레거시 사촌** — 같은 평면(ESME↔SMSC, A2P 대량발송·OTP·마케팅·**스미싱 캠페인 주입**)을 다른 와이어로 나르는 여전히 현역인 구형 SMSC 접속 프로토콜(CMG/LogicaCMG); SMPP 가 고정 16바이트 바이너리라면 UCP 는 **ASCII 텍스트 프레임**(``STX``~``ETX`` 사이 ``/`` 구분 필드열)이라 :mod:`forensiclab.ftp`·:mod:`forensiclab.sip` 처럼 평문—``TRN``(요청↔응답 상관 smpp sequence_number 대응)/``LEN``/``O_R``(O 연산·R 결과)/``OT``(01 call input·51 submit·52/53 delivery·**60 session management 로그인**); **자격증명 노출**(OT 60: ``account``(OAdC)·**평문 ``password``**(PWD) 그대로—smpp bind system_id/password·:mod:`forensiclab.ftp` 평문 로그인급, 결과 ``N``(NACK)+``error_code`` 반복=패스워드 추정 RADIUS Access-Reject 대응)·**당사자·본문**(OT 01/51: ``recipient``(AdC 착신 표적)·``originator``(OAdC 발신 위조 발신자명/숏코드), OT 01 은 고정 위치라 ``message`` 까지—MT=3 이면 IRA 16진→텍스트 스미싱 링크·OTP 자체, OT 51 은 깊고 버전의존이라 첫 두 필드만 보수적)·**연산·결과·무결성**(``operation_name``/``is_operation``/``is_result`` 로그인 60→submit 51 :mod:`forensiclab.timeline`·``trn`` :mod:`forensiclab.flows` IP쌍 안 상관·``is_ack``/``is_nack``·체크섬 합 검증 ``checksum_ok``); ``STX`` 로 시작 안 함·필드 5개 미만·``O_R`` 이 O/R 아님·``OT`` 2자리 숫자 아님이면 TCP 스트림 오탐 가드 ``None``·``ETX`` 없는 절단이면 가용분까지·체크섬 검증 불가 ``None``·decode_ucp_ira/operation_name 공개헬퍼·읽기 전용 무변형·offset 지원) 코어.
- :mod:`forensiclab.cimd2` — CIMD2(Computer Interface to Message Distribution v2) 파싱(Nokia SMSC 접속 프로토콜; :mod:`forensiclab.smpp`(현대 표준)·:mod:`forensiclab.ucp`(CMG/LogicaCMG 레거시)에 이은 **세 번째 SMSC 접속 형제** — 셋 다 같은 평면(ESME↔SMSC, A2P 대량발송·OTP·마케팅·**스미싱 캠페인 주입**)을 다른 와이어로 나름; :mod:`forensiclab.ucp` 처럼 **ASCII 텍스트 프레임**(``STX``~``ETX``)이지만 UCP 가 ``/`` 구분 *위치 기반* 필드열이라면 CIMD2 는 ``TAB``(0x09) 구분 **``코드:값`` 쌍**(parameter list)—프레임 ``<STX> OC:PN <TAB> PC:PV <TAB>… [CC] <ETX>``; **OC**(operation code 2자리: 01 login·02 logout·03 submit message·10 deliver MO·40 alive, **응답=요청+50** login→51 submit→53, 98 NACK·99 general error)·**PN**(packet number 요청↔응답 상관, ucp ``trn``·smpp ``sequence_number`` 대응)·**PC:PV**(parameter 3자리 코드+값, 마지막 TAB 뒤 선택적 체크섬 2자리 16진); **자격증명 노출**(login 01: ``user_identity``(010)·**평문 ``password``**(011) 그대로—smpp bind·ucp 세션 PWD·:mod:`forensiclab.ftp` 평문 로그인급, login 응답(51) ``error_code``(900) 반복=패스워드 추정 RADIUS Access-Reject 대응)·**당사자·본문**(submit 03/deliver 10: ``recipient``(021 destination 착신 표적)·``originator``(023, 없으면 027 alphanumeric 위조 발신자명/숏코드)·``message``(033 user data 스미싱 링크·OTP 자체))·**연산·결과·무결성**(``operation_name``/``is_request``/``is_response`` login→submit→logout :mod:`forensiclab.timeline`·``packet_number`` :mod:`forensiclab.flows` IP쌍 안 상관·``is_nack``/``is_error``/``error_code``·STX부터 마지막 TAB까지 합 검증 ``checksum_ok``·``get(code)`` 임의 파라미터 접근); ``STX`` 로 시작 안 함·헤더가 ``숫자:숫자`` 꼴 아님·operation code 가 알려진 CIMD2 연산(닫힌 열거) 아님이면 TCP 스트림 오탐 가드 ``None``·``ETX`` 없는 절단이면 가용분까지·체크섬 없거나 검증 불가 ``None``·operation_name/parameter_name 공개헬퍼·읽기 전용 무변형·offset 지원) 코어.
- :mod:`forensiclab.mqtt` — MQTT(MQTT Control Packet) 파싱(OASIS MQTT 3.1.1·일부 5.0·흔히 TCP 1883 평문/8883 TLS; :mod:`forensiclab.smpp`·:mod:`forensiclab.ucp`·:mod:`forensiclab.cimd2` 가 통신사 SMSC 접속(ESME↔SMSC) 메시징 평면이라면 MQTT 는 **사물인터넷(IoT) 메시징 평면** — 센서·게이트웨이·앱이 브로커(broker)에 붙어 ``topic`` 으로 발행/구독하는 경량 pub/sub, ForensicLab IoT 센서 모니터링 대상과 직접 맞닿고 봇넷 C2·ICS 텔레메트리·스마트홈 침해 분석에서 자주 등장; :mod:`forensiclab.smpp` 처럼 TCP 위 **바이너리 고정 헤더+본문** — 모든 패킷이 고정 헤더(1바이트 상위 4비트=패킷 종류 1 CONNECT·2 CONNACK·3 PUBLISH·4 PUBACK·8 SUBSCRIBE·12 PINGREQ·14 DISCONNECT…·하위 4비트=플래그, PUBLISH 만 DUP/QoS/RETAIN 의미·그 외는 예약 고정값)+**Remaining Length**(1~4바이트 가변정수, 하위 7비트씩·최상위 비트 연속 표시)로 시작, 구조 아는 패킷만 깊게 풀고 나머지는 ``payload_offset`` 으로만; **CONNECT**(1) 프로토콜 이름(``MQTT`` v3.1.1·``MQIsdp`` v3.1)·레벨·connect 플래그(username/password/will/clean)+keep-alive, 페이로드에 ``client_id``·(will 토픽/메시지)·**``username``·평문 ``password``**(:mod:`forensiclab.ftp`·:mod:`forensiclab.smpp` 평문 로그인급 노출—1883 평문 브로커면 자격증명 그대로)·**CONNACK**(2) session-present+**return code**(0 수락·4 bad username/password·5 not authorized, ``is_refused`` 반복=브루트포스 RADIUS Access-Reject·SMPP ESME_RINVPASWD 대응)·**PUBLISH**(3) ``topic``(계층 구조가 곧 IoT 자산·명령 채널)+QoS>0 이면 ``packet_id``+페이로드(센서값·명령·C2, ``payload_offset`` 만)·**PUBACK/PUBREC/PUBREL/PUBCOMP/SUBACK/UNSUBACK** ``packet_id`` 요청↔응답 상관(:mod:`forensiclab.flows` IP쌍, SMPP sequence_number 대응); **자격증명·브루트포스**(CONNECT username/평문 password·CONNACK return 4/5 반복)·**IoT 자산·C2**(PUBLISH topic/SUBSCRIBE 토픽 필터)·**세션 상관·타임라인**(``client_id``·``packet_id``·CONNECT→PUBLISH→DISCONNECT :mod:`forensiclab.timeline`)·**MQTT 5.0 properties** 블록은 길이만큼 건너뜀; 패킷 종류 0(예약)·PUBLISH 외 예약 플래그가 규격값과 다름(PUBREL/SUBSCRIBE/UNSUBSCRIBE 0b0010·그 외 0)·PUBLISH QoS 3·Remaining Length 가변정수 4바이트 초과·CONNECT 프로토콜 이름이 ``MQTT``/``MQIsdp`` 아님이면 TCP 스트림 오탐 가드 ``None``·Remaining Length 가 가용분 초과면(절단) 풀 수 있는 만큼만 ``truncated=True``·packet_type_name/connack_return_name 공개헬퍼·읽기 전용 무변형·offset 지원) 코어.
- :mod:`forensiclab.coap` — CoAP(Constrained Application Protocol) 메시지 파싱(RFC 7252·일부 7641 Observe·7959 Block·흔히 UDP 5683 평문/5684 DTLS; :mod:`forensiclab.mqtt` 가 IoT 기기가 브로커에 붙어 ``topic`` 으로 발행/구독하는 **pub/sub 메시징 평면**이라면 CoAP 는 같은 사물인터넷 세계의 **REST 평면** — 제약 장치(센서·액추에이터)를 위해 HTTP 를 UDP 위 경량 바이너리로 줄인 요청/응답이라 :mod:`forensiclab.http` 의 IoT 사촌, MQTT 가 메시지를 흘려보낸다면 CoAP 는 ``GET /sensors/temp`` 처럼 **자원(URI)에 메서드를 건다**; ForensicLab IoT 센서 모니터링 대상과 직접 맞닿고 봇넷 C2·ICS 원격 제어·**CoAP 증폭 반사 DDoS**(작은 요청→큰 응답) 분석에서 자주 등장; :mod:`forensiclab.mqtt` 처럼 **바이너리 고정 헤더+가변부**이나 UDP 위라 한 데이터그램이 곧 한 메시지 — **4바이트 고정 헤더**(첫 바이트 Version<상위 2비트 항상 1>·Type<2비트: 0 CON 확인 요구·1 NON·2 ACK·3 RST>·**TKL**<하위 4비트 Token 길이 0~8, 9~15 형식 오류>, 둘째 바이트 **Code**<상위 3비트 class·하위 5비트 detail: 0.01 GET·0.02 POST·0.03 PUT·0.04 DELETE·2.05 Content·4.04 Not Found…>, 3·4바이트 **Message ID**<CON↔ACK 상관·재전송 중복 탐지>)+**Token**(TKL 바이트, 요청↔응답 상관 MQTT packet_id·SMPP sequence_number 대응)+**옵션들**(각 1바이트 상위 4비트 델타·하위 4비트 길이, 13/14 확장 바이트, 번호는 누적 델타)+``0xFF`` 페이로드 마커 뒤 본문; 구조 아는 부분만 풀고 본문은 ``payload_offset`` 으로만; **IoT 자원·C2**(Code 메서드+**Uri-Path(11)**(``/`` 이어 ``uri_path``)·**Uri-Query(15)**·**Uri-Host(3)** 가 HTTP 요청 라인처럼 "무엇을 어디에"—``GET /.well-known/core`` 자원 정찰·``PUT /actuator`` 제어·비표준 경로 C2, **Content-Format(12)**·**Observe(6)** 구독·**Block1/2(27/23)** 등 열거)·**증폭 반사 DDoS**(작은 CON GET 에 큰 응답·위조 출처 반사, ``is_request``/``code_name`` 과 응답 크기 상관)·**세션·타임라인**(``message_id``·``token`` 으로 CON→ACK·요청→응답 :mod:`forensiclab.timeline`·``is_confirmable``/``is_ack``/``is_reset``); ``code_name`` ``c.dd Name`` 표기·``get_option(번호)``·옵션 ``as_uint``/``as_text`` 편의; 4바이트 미만·Version≠1·TKL>8·Code class 예약(1·3·6·7)·Token 절단·옵션 델타/길이 니블 15(0xFF 마커 아닌데)면 UDP 스트림 오탐 가드 ``None``·옵션 값이 가용 바이트 초과면(절단) 풀 수 있는 만큼만 ``truncated=True``·coap_type_name/coap_code_name/option_name/content_format_name 공개헬퍼·읽기 전용 무변형·offset 지원) 코어.
"""

__version__ = "0.94.0"

__all__ = ["__version__"]
